# 专利无效与现有技术检索系统项目宪章

> 状态：强制执行
>
> 版本：2.42
>
> 生效日期：2026-08-23
>
> 强制预读范围：本文件所在的整个 `patent` 项目
>
> 产品约束范围：专利稳定性/无效检索系统，以及为了接入该系统而修改的共享前端、会话、模块一、数据库和运维边界
>
> 当前阶段：已获用户授权，进入数据源验证、业务实现与全量实例回归阶段

## 0. 强制预读规则

本文件是本项目所有后续工作的产品边界和架构基线。

1. 每个新的工作回合或独立任务，在首次读取、搜索、修改项目文件，查看 Git 状态、日志，运行测试或启动服务之前，必须完整阅读本文件一次。
2. 同一工作回合内无需在每次打开单个文件前重复阅读；但如果本文件在本回合被修改，修改完成后必须重新完整阅读一次，随后才能继续处理其他文件。
3. 预读后应先判断当前请求是否符合本文件的目标、范围、限制和触发标准。存在冲突时停止扩大实现范围，先向用户说明冲突并取得明确决定。
4. 任何改变产品目标、法律分析边界、MVP 范围、核心数据源、模型能力、生产/测试隔离方式或禁止依赖的决定，必须先更新本文件，再修改业务代码。
5. 本文件不取代用户的最新明确指令；用户改变方向时，应先把新决定写入本文件并形成版本记录。

### 0.1 与现有项目说明文件的关系

强制预读适用于整个 `patent` 目录，是为了避免无效检索开发误伤现有侵权系统；但本文件的无效检索业务规则不自动改写原侵权项目的既有行为。

读取和适用顺序为：

1. 每个新工作回合先完整阅读本文件，确认产品目标和边界。
2. 再阅读根目录 `AGENTS.md`，了解仓库当前状态、已有改动、运行方式和全局工程约束。
3. 无效检索及其共享接入任务，在进入具体模块前先完整阅读 `docs/invalidity/WORKFLOW_SPEC.md`，再阅读距离目标文件最近的模块级 `AGENTS.md` 和相关 README；与无效检索无关的任务直接进入相应模块说明。
4. 无效检索及其共享接入改动，以本文件的产品目标、法律边界和范围决定为准；详细业务状态机和数据契约以工作流规范为准；具体仓库运行和局部实现约束以根级、模块级 `AGENTS.md` 为准。
5. 与无效检索无关的原侵权系统维护，以根级和模块级 `AGENTS.md` 为准；不得借本文件要求顺便重构原系统或删除其已验证依赖。
6. 下级说明可以增加更严格的局部约束，但不得静默改变本文件已经确认的无效检索产品边界。出现真实冲突时先停止修改，向用户说明冲突并先修正文档。
7. 会话日志中的旧决定与更新后的正式规则冲突时，以最新明确决定为准，并应同步修正旧日志的过时表述。

### 0.2 唯一规则层级与作用域

仓库只保留一份产品宪章，不在测试版、正式版或子模块中复制第二份“主宪章”。读取顺序是为了先获得上下文，不等于冲突时的权威顺序；发生不一致时按作用域采用以下层级：

1. 用户最新明确指令优先；如改变产品边界，应先更新本文件再改代码。
2. 本文件决定无效检索的产品目标、法律边界、MVP 范围、证据标准、单后端边界和完成门槛。
3. 在无效检索及其共享接入范围内，`docs/invalidity/WORKFLOW_SPEC.md` 决定本文件 4.2、4.4、4.6 和 4.9 的详细状态机、模块契约和持久化不变量；它不得改变本文件的法域、产品或证据边界。
4. 根目录 `AGENTS.md` 决定仓库工程约束、运行方式和当前事实；其中“模块一到模块四单向调用、不得循环”只约束原侵权分析主链路，不限制无效检索由 `I0` 驱动的持久化轮次。
5. 距离目标文件最近的模块级 `AGENTS.md` 决定局部实现约束，可以增加更严格的安全、测试和运行要求，但不得改写本文件或工作流规范的无效检索业务语义。
6. README 只说明使用和运行方式；会话日志只记录历史。两者都不得覆盖上述正式规则。

自 2.37 起，每项业务只保留一套权威后端，不再按“正式页面/测试页面”复制第二套服务、schema、
token、上传根、工件或 release。专利无效以后续一直调试的无效模块链为唯一后端；Agent 与
`/test/module-lab` 只是在同一后端之上的完整流程视图和逐模块实验视图。专利分析同样以后续一直
调试的四模块链为唯一后端；Agent 与 `/test/product-pipeline` 只改变编排和展示，不改变模块实现、
模型、规则、数据表或 provider。历史 `test/prod` 名称、5109 release 和兼容变量可以暂时保留用于
迁移审计，但不得再被任何用户入口选择为另一套业务后端。侵权业务与无效业务、不同用户、不同
会话和不同运行版本仍必须使用明确隔离键。

## 1. 我们要开发什么

在现有专利侵权检索/分析产品基础上，增加一套面向中国专利无效和稳定性评估的模块化系统。

系统与现有产品共用登录、上传能力和前端外壳。正式用户入口采用单一对话式专利 Agent：页面主操作区
只有一组支持文本和文件附件的消息输入，并在附件按钮旁提供“专利无效”和“专利侵权分析”两个可独立
切换的显式工作流开关。两个开关都可以选择，也可以都不选择；只有用户点击选中的分析类型才允许创建
对应后台任务。两者均未选择时，无论消息文字是否像分析请求，都只进入普通大模型问答，不创建侵权或
无效任务。现有侵权与无效后台模块、数据表、状态机和证据边界保持不变，Agent 只作为正式前端的显式
工具编排、一般问答、进度恢复和结果解释层。

无效检索系统以中国现行专利法律规则为分析框架，但现有技术检索覆盖全球，不限于专利文献。MVP 应覆盖：

- 全球专利、专利申请及其专利族、引证和相关法律事件；
- 学术论文、会议论文、预印本、学位论文；
- 国家标准、行业标准、技术规范及可证明公开日期的标准草案；
- 产品手册、用户手册、维修手册、白皮书、目录、宣传册；
- datasheet、应用笔记、设计指南和厂商技术资料；
- 可验证公开时间的网页、论坛帖子、邮件列表、软件文档、源代码和发布记录；
- 用户人工导入的其他潜在现有技术材料。

系统完成权利要求解析、关键日判断、检索策略生成、多源检索、证据标准化、新颖性分析、创造性分析，并输出可追溯的权利要求对照表（CC 表）及检索审计记录。

系统输出是检索和分析辅助材料，不是专利无效决定、正式法律意见或行政机关结论。

## 2. 解决的核心问题

系统重点解决以下问题：

1. 通读目标专利后，把检索信息区分为“保护客体/技术类别”“类别共有技术语境”“体现发明点的区别性特征”，并为每个词组选择标题摘要、权利要求或说明书全文等最可能出现的检索范围；避免把全部权利要求限制机械等价为同一层级的“必要特征”，也避免用孤立、宽泛词语造成无效搜索。检索规划还应从目标专利本身提取部件角色、连接拓扑、运动/状态变化和控制关系，形成不依赖某一决定书或已知公开号的通用机构工作模型；已知决定书及其对比文件只能作为运行后隔离评估的盲测答案，禁止进入生产查询生成输入或专用词表。
2. 为当前范围内的每项独立权利要求记录专利级关键日，并用确定性规则判断候选材料能否用于新颖性、创造性或只能作为追溯线索。
3. 跨语言、跨国家、跨专利与非专利来源召回相关现有技术，减少单一数据库覆盖不足造成的遗漏。
4. 将搜索结果转化为可核查证据：保存原文、译文、来源、公开时间依据、页码/段落/权利要求/附图位置和内容哈希。
5. 严格区分：
   - 普通现有技术：在目标权利要求关键日前已向公众公开，可用于新颖性和创造性；
   - 抵触申请候选：符合中国规则的更早申请、之后公开的申请，只进入新颖性分析；
   - 后公开线索：只用于术语、引证和更早来源追溯，不直接支持无效理由。
6. 对新颖性坚持“一项权利要求对一份对比文件”的完整披露分析；对创造性单独记录最接近现有技术、区别特征、技术问题、组合启示和反向教导。
7. 让律师能够按“当前真实案件”或“内置示例”两种方式单独测试业务模块，并用通俗语言观察输入、输出、失败原因和耗时；人工 JSON、模型输出和底层运行记录只保留在高级技术入口。
8. 在与现有侵权系统共享前端和数据库的同时，避免会话、端口、运行记录、结果表和测试环境互相污染。
9. 把“已经取得原始 PDF/XML”与“已经形成可供实体比对的完整可读全文”分开管理；扫描件只有在全页文本覆盖或受控 OCR 完成并保存逐页来源后才允许进入确定性的未披露判断。

## 3. 服务的用户

### 3.1 核心用户

- 中国专利诉讼和无效程序中的律师、专利代理师及其团队；
- 企业知识产权、研发和产品合规团队；
- 需要开展专利稳定性初筛和对比文件调查的专业检索人员。

### 3.2 用户能力假设

- 用户理解权利要求、新颖性、创造性、优先权和对比文件等基本概念；
- 用户需要系统减少机械检索和逐项制表工作，但仍会进行专业复核；
- 用户可以补充优先权信息、导入缺失文献、确认关键日和修正模型拆解结果。

### 3.3 非目标用户

MVP 不面向完全无人复核的消费者自助法律结论，也不承诺取代律师、代理师、无效审查部门或专业查新机构。

## 4. MVP 的架构原则

### 4.1 共用事实层，隔离业务层

- 共用前端外壳、用户体系、上传能力和中立的专利事实解析模块；正式根入口为同一个对话式专利 Agent。
  普通用户可见的测试入口固定收敛为两个大型模块实验室：①现有 11 模块的高级无效检测实验室；
  ②覆盖现有专利分析模块的专利分析实验室。两个入口并列放在 Agent 侧边栏同一位置，不再展示
  其他无效测试页或零散测试入口。既有细分 `/test/**` 路由如仍被契约测试、内部调试或深链使用，
  可以保留兼容，但不得继续作为普通用户导航或第三个大型测试产品入口。
- 专利分析实验室固定采用与无效检测实验室一致的模块化测试结构：页面顶部只有一个当前测试专利
  输入区，下面依次展示专利解析、关键词生成、商品检索、权利要求与商品比对四个业务模块。每个
  模块可以单独运行，必须分别展示该次运行实际消费的输入、产生的输出、状态和失败原因；模块外层
  以及长输入/输出默认收起。后序模块只消费同一测试专利和同一测试会话中已形成的前序结果，不得
  静默跨会话读取历史结果；高级参数可以保留在折叠区，但不得继续占据页面主视图。页面只保留这一
  个专利分析实验室，不把模块一、搜索、商品或其他细分测试页继续作为并列测试产品；四个阶段直接
  使用业务名称，不显示“模块1结果”等技术编号标题。每个阶段须把“本次输入”“本次输出”和“结果”
  分开呈现，结果使用与无效实验室一致的卡片层级和可读状态样式。
- 侵权 Agent 的管理员过程卡必须提供当前 `analysis_session_id` 的同任务诊断入口，直达
  `/test/product-pipeline?session=<analysis_session_id>`。带 session 的专利分析实验室是管理员
  只读诊断模式：只恢复该 session 已持久化的专利解析、关键词生成、商品检索、权利要求与商品比对
  四阶段真实输入、输出、结果、状态和错误；查看、轮询或刷新不得创建新测试会话、重新调用模块、
  改写原 session，或从其他 session/run 静默补入结果。无 session 时仍保留现有显式逐阶段实验能力。
- 首页输入区以“专利无效”和“专利侵权分析”两个可多选按钮作为唯一后台工作流开关；Agent 必须把
  用户选中的类型确定性落为 `analysis_kind`，明确区分 `infringement`、`invalidity`。同时选择则创建
  两个独立运行并分别保存 ID、状态和结果，不得合并运行记录；两者均未选择时不得调用任何分析工作流。
- 无效检索使用独立的查询、候选文献、日期资格、分析和 CC 结果表；不得复用商品检索或商品侵权评分表表达无效分析。
- 数据库可以共用；不再按前端页面区分生产/测试后端，但侵权/无效、不同用户、不同会话和不同运行版本必须有明确隔离键。

### 4.1.1 正式对话式专利 Agent 边界

1. 正式页面主交互固定为消息流和一组可上传文件的输入框；附件入口旁设置“专利无效”和“专利侵权
   分析”两个可多选按钮。按钮只选择是否启动相应完整后台流程，不要求用户理解或逐项操作后台模块。
   输入可以是普通专利问题、专利文本、专利号/URL，或 PDF/TXT/HTML 等现有上传链支持的文件；文本需求
   与附件属于同一条用户消息并共同参与意图识别。
2. Agent 的执行类型由两个按钮确定：均未选为 `question`，只选侵权为 `infringement`，只选无效为
   `invalidity`，同时选择为 `both`。服务端必须把按钮状态作为权威输入再次校验，不能根据消息文字、
   模型意图或附件内容自动增加未选择的工作流。普通问答不创建分析会话或调用两条后台工具；已选择分析
   但缺少运行所必需的专利输入时，应在同一对话中询问补充，不得创建空任务。
3. Agent 只允许调用版本化工具白名单：读取当前附件/文本事实、创建或恢复侵权分析、创建或恢复无效
   调查、查询进度、读取已授权结果。模型不得生成任意内部 URL、SQL、shell 命令、provider 参数或跨用户
   ID；所有工具参数由服务端 schema、当前登录用户、文件归属和分析类型再次校验。
4. 侵权与无效后台继续按各自既有 HTTP/数据库合同执行。Agent 不把两个状态机搬进模型 prompt，不以
   进程内递归代替持久化任务，也不因对话断开而取消后台运行；每个工具调用保存会话、消息、用户、附件、
   意图、参数摘要、目标分析 ID、状态和错误，刷新后可恢复。
5. 直接问答可以由当前获准的 GLM 模型生成，但必须明确其为一般专利信息辅助，不冒充正式法律意见；引用
   用户附件内容时须区分原文事实与解释。涉及当前案件分析结论时优先读取已持久化工具结果，不得在后台
   尚未完成时凭模型补造。
6. 对话界面必须在消息流中分别展示解析、侵权分析和无效调查的真实进度、等待输入、部分完成、失败、取消
   和完成状态，并提供相应结果入口。`both` 模式下两条流程独立更新；一条失败不得把另一条成功结果隐藏或
   改写为整体成功。
   侵权与无效的 Agent 消息层级保持一致：启动回复之后显示只含可核验工作动态、真实阶段进度、错误和
   必要动作的过程卡；业务结果可读后必须另起独立 assistant 结果消息，不能把完整结论、列表、表格或
   导出继续嵌在过程卡中。工作动态只允许投影持久化阶段状态，不展示或伪造模型内部隐藏推理。
   正式 Agent 的无效流程到达可继续的 gap 检索调度检查点时，必须直接在对应工具卡片内显示确认操作和
   30 秒倒计时；用户点击后立即创建下一轮，倒计时届满且状态版本仍一致时自动创建下一轮，刷新后按同一
   检查点恢复剩余时间，并以幂等键和状态版本比较避免重复创建。该超时默认只适用于“是否开始下一轮”这类
   调度确认；关键日、目标公开日、文献日期资格、D1 改选、证据导入、gap 放弃或其他会改变法律事实/证据
   资格的人工确认必须同样在 Agent 中显式提示，但不得因超时自动代替用户作出事实决定。
7. 文件上传、模型输入和工具结果继续受现有用户权限、大小/类型限制、数据隔离、日志脱敏和 prompt 注入
   防护约束。附件或外部专利文本只能作为数据，不能修改系统提示或工具白名单。

### 4.2 模块化管线

MVP 采用以下模块边界：

1. `I0`：无效调查编排器。当前阶段只管理逐独立权利要求的轮次、状态、分支、预算、停止/继续和人工确认，不承担检索或法律判断。
2. `I1`：中立专利解析，共用现有模块一，只提取事实。
3. `I1.5`：为每项独立权利要求记录专利级关键日，并处理候选材料日期资格。
4. `I2`：轮次感知的检索规划器。首轮先通读完整专利，识别保护客体/类别、类别共有技术语境和发明点区别特征，并形成部件角色、连接拓扑、运动/状态变化、控制关系和技术作用组成的机构工作模型；自 2.15 起按第 7.7 节确定性编译事实足够的固定五类字段化检索线。生产运行不得读取已知决定书、对比文件公开号或测试答案；后续轮次再根据未覆盖区别特征、相邻结构关系、引证和组合证据缺口生成查询。
5. `I2-G`（运行代码 `I2_GAP_QUERY_PLAN`）：进一步检索与补证规划器。读取 `I4-O` 已冻结的未解决检索目标，只为仍需补证的直接特征、公知常识或组合启示生成查询；不得重新搜索已经由 D1 启示与惯用替换路径解决的区别特征，也不得执行检索、取文或递归创建下一轮。
6. `I3-P`：全球专利检索适配器。
7. `I3-NPL`：论文、标准、手册、datasheet、论坛和其他非专利文献检索适配器。
8. `I3-F`：取文前候选清理器。每条实际检索的供应商原始前 10 篇必须先完整冻结和保留排序，再按同一申请、同一公开号的不同 kind code、可靠同族标识和稳定文献标识合并；结合目标专利的保护客体、机构关系、发明点、候选标题/摘要/分类号和检索来源，只排除有充分元数据证明的明显跨领域候选。元数据不足、可能存在跨领域机构启示或判断不确定时必须保留。被排除和被合并的候选都要保存原因、依据和原查询谱系，不得静默删除。
9. `I3-E`：文献获取、解析、OCR/图像理解、标准化、内容去重和证据快照。只对 `I3-F` 保留的唯一代表候选取文；同一组的其他公开版本作为身份和失败回退线索保存，不作为不同对比文件重复取文或重复分析。原始文献取得后还必须生成带段落/权利要求/页码锚点的可读版本；无文本层页面异步 OCR 并按内容哈希缓存，未达到可分析覆盖时不得进入确定性否定判断。
10. `I4-S`：单文献比对器。每一轮新取得且达到可分析覆盖的每篇材料，都先单独与目标权利要求全部特征比对，用于新颖性检查和覆盖矩阵。比对先分别理解目标权利要求与对比文件的整体机构和工作过程，再按部件角色、连接拓扑、运动/状态和结构作用映射特征；名称不同不自动否定披露，仅功能相同也不足以确认披露。
11. `I4-C`：最接近现有技术选择与区别特征提取器。按已确认相同技术特征数量作为第一排序标准选择候选；并列时再比较技术领域/用途、技术作用、结构改动、证据完整度和日期资格。保存选择理由和历史版本，并输出带目标内容、结构角色和技术作用的区别特征集合。
12. `I4-O`（运行代码 `I4_O_OBVIOUSNESS_PRECHECK`）：显而易见性预分析器。对 D1 未直接公开的区别特征或相互配合的特征组，先分析客观技术问题、D1 内部具体启示、惯用技术手段/公知常识、改造动机、反向教导和技术效果是否可预期，再决定免除普通结构检索、仅补公知常识证据、检索组合启示、检索直接特征证据或转人工复核。区别特征身份与显而易见性预判必须分开保存；模型预判可以分流检索，但没有可引用证据或律师确认时不得冒充证据完整的最终结论。
13. `I4-I`：创造性组合分析器。基于最新 `I4-O`、模块九最多五轮取得且已完成日期资格与 `I4-S` 的全部当前语料，按创造性判断三步法评估 D1+D2/D3、D1+公知常识或 D1 内部启示+惯用替换三类路径。输出必须先固定 D1，再逐项列明 D1 区别特征与实际解决的技术问题，并为每项区别特征建立“补充文献/公知常识是否公开、是否承担相同结构角色与作用、具体引文、结合启示/修改动机与路径、反向教导、效果可预期性”的证据闭环矩阵。不得只用全局模板或累计覆盖数量代替逐区别特征分析。
14. `I5`：律师报告固定包含三部分：第一部分把当前 `I4-I` 三步法结构化结论转化为可供律师阅读的文字分析，逐项保留 D1、区别特征、实际技术问题、补充现有技术、公知常识、技术启示、修改动机与路径、反向教导、效果可预期性和证据不足限定，不得补造模块十没有的事实或把辅助意见写成行政结论；第二部分为相似度最高的 10 篇对比文件横向特征矩阵，以目标独立权利要求全部特征为纵轴、10 篇文献为横轴，显示披露/未披露/待确认/分析失败状态。这里的相似度以当前版本 `I4-S` 已确认披露特征数量和覆盖比例为主，待确认不算披露，并结合证据完整度等确定性字段排序，不使用文本向量替代法律事实；不足 10 篇时如实展示实际数量。第三部分保留全部检索到且进入逐篇分析的对比文件与该独立权利要求的完整比对、引文、位置、日期和来源状态。三部分均须绑定当前单次业务运行谱系。

每个模块必须具备版本化输入/输出契约、独立运行接口、运行 ID、状态查询、取消/重试能力和固定测试夹具。模块不得依靠前一模块的进程内状态才能重放，也不得直接递归调用下一轮；循环由 `I0` 根据持久化结果推进。

`I4-S` 多文献比对属于可持续数十分钟的持久化批次，不得由浏览器单次请求或固定十分钟倒计时决定成败。批次必须逐篇保存文献、module run、排队/执行/终态和可用输出，刷新、断网或前端超时后可以按批次 ID 恢复；已完成的单篇结果不得因其他文献仍在排队、运行或失败而从业务结果中消失。排队时间与实际执行时间应分别展示，客户端只有在后端返回终态时才把该篇标为完成或失败。测试服务允许通过数据库租约启动受控的多个 durable worker 并行处理相互独立的单文献任务，默认并发 2、环境变量可调但硬上限 4；每个 worker 必须有独立身份和活动租约，停机时逐 worker 释放，禁止用前端并发制造无持久化的进程内任务。律师模块实验室第⑥阶段的批次只执行 `I4-S`，不得顺带创建第⑦阶段 `I4-C` 或第⑩阶段 `I4-I`。

历史上由旧前端直接创建、没有批次 ID 的 `I4-S` 运行，恢复时不得仅按创建时间间隔猜测批次范围。恢复清单必须以同一案件、同一独立权利要求下最新 `I3_FETCH` 的持久化检索血缘为准：优先选择共享同一 `candidate_filter_run_id` 且确有 `analysis_ready=true`、正文和内容哈希的全文文献；对清单中已有的单篇比对复用最新 run，对尚未创建比对的文献补建独立 run。这样既不能把相邻检索轮次混入，也不能因旧页面提前停止创建任务而漏掉后续全文。

律师模块实验室采用十一个可分别测试的业务阶段，不再把 D1、显而易见性预分析、补证循环和最终组合分析折叠在同一张卡片中：①读取目标专利；②确定关键日；③总结核心发明点；④生成首轮检索词；⑤首轮检索与证据核验；⑥单篇新颖性比对；⑦选择 D1 与冻结区别特征；⑧显而易见性预分析；⑨进一步检索与补证循环；⑩创造性组合分析；⑪大 Claim Chart 与律师报告。每个阶段都必须同时提供“当前真实案件”和“内置示例”测试入口、独立运行状态和可读输入/输出；存在前置事实要求的阶段应明确提示并消费同案已持久化前序结果，禁止用客户端伪造事实绕过后端守门。第⑨阶段是一个可展开的有界循环业务阶段，内部按轮次执行 `I2-G → I3 → I4-S → 覆盖复核`，不得把五个 gap 轮各自伪装成五个固定顶层模块。为避免一次页面操作连续等待数十分钟，每次用户操作最多推进一个 gap 轮：当轮全部任务持久化收口后暂停，页面分轮展示并由用户显式点击“继续下一轮”。这只是律师界面的可恢复调度边界，不改变一项调查最多五轮的业务上限。

### 4.3 证据优先

- 搜索命中不是证据；只有取得可验证内容及来源后才能进入 CC 表。
- 对每项证据保存原始文件或合规快照、获取时间、来源 URL/供应商 ID、内容哈希和公开日期依据。
- 对论坛、网页和动态内容，不能证明公开时间或内容稳定性的，只能作为调查线索。
- OCR、翻译和模型摘要不得覆盖原文；必须保存原文和置信度。
- 来源不因“官方”自动成为合格证据，也不因“第三方”自动失去资格。系统按以下层级管理材料：
  - `lead`：搜索结果、摘要、片段或指向潜在文献的线索；
  - `retrieved_document`：已经取得真实全文、PDF、页面、图片或可重放原始内容，但日期或来源资格尚未全部确认；
  - `qualified_evidence`：真实内容、目标技术披露、公开时间和来源链均已核验，可以进入 CC 表。
- Google Patents、第三方数据库、MCP、普通网页检索和 Playwright 均可用于发现或取得真实材料；是否成为 `qualified_evidence` 由证据校验决定，而不是由获取工具名称决定。

### 4.4 日期规则先于模型判断

- 日期资格由可测试的确定性规则引擎处理，不由大模型自行决定。
- 关键日以专利为单位确定：MVP 阶段整件专利只取一个关键日（有优先权日取最早优先权日，否则取申请日）。模块一继续冻结全部权利要求事实，但当前自动调查只把该日期写入并适用于独立权利要求；只有专利级日期完全缺失或无法解析时才转人工复核。多优先权、逐主题分别确定关键日的精细化规则留待用户明确恢复。
- 关键日之后首次公开的普通文献不得进入创造性分析。
- 关键日后公开的材料只有在满足抵触申请条件时才可进入对应的新颖性路径；其他材料只能作为追溯线索。
- 专利发现必须显式拆成两个日期通道：普通现有技术通道按公开日在关键日前检索；中国抵触申请候选通道按更早申请/优先权事实及用户确认的目标公开日前窗口检索。provider 端过滤只能减少召回量，最终资格仍由本地确定性规则按权利要求判定。
- 非专利文献不进入抵触申请通道；关键日后首次公开的论文、标准、手册、datasheet、网页或论坛材料只能作为追溯线索。
- 日期、优先权或公开可得性无法确认时，系统必须显示“待人工复核”，不能推定为合格证据。

### 4.5 多源适配，不绑定单一平台

- MCP 是传输/工具协议，不是数据源本身。核心数据模型不得依赖某一个 MCP 的返回字段。
- 专利来源、论文来源、标准来源、网页来源分别使用 provider adapter，并保留原始响应。
- 官方 API、商业/第三方 API、公开数据集、Google Patents、浏览器自动化、普通 HTTP 获取和人工导入都是允许的 provider 形态，应通过同一证据标准评估。
- 搜索提供商不可用时允许任务部分完成或切换经批准的备用源，但禁止静默改变检索范围。
- 人工导入是一级能力，不是异常兜底；任何 API 覆盖不足时都应允许补充文献。
- 自动专利候选发现与候选全文取得可以由不同 provider 承担。当前专利候选仍由智慧芽
  P002 发现；智慧芽返回具有可核验公开号/申请号的 lead 后，允许以 EPO OPS 作为官方
  书目核验和全文/PDF/页面补取源。该补取不得改变智慧芽候选集合、扩大检索范围或冒充
  智慧芽全文接口成功；两次 provider 调用及其标识映射必须分别审计。

### 4.6 按权利要求反复检索，并分离新颖性与创造性

无效检索不是一次性“搜索后统一打分”，而是对每项独立权利要求分别运行的有界循环。不同独立权利要求可以共享已取得文献，但必须分别保存覆盖结果、关键日、最接近现有技术和停止原因。模块一仍解析、保存和冻结全部独立及从属权利要求；当前阶段只为独立权利要求创建 `ClaimInvestigation`，从属权利要求不进入自动检索、分析、状态汇总或报告结论。

每项独立权利要求按以下顺序推进：

1. **首轮检索**：先通读完整权利要求、摘要、背景技术、发明内容、具体实施方式和附图，识别保护客体/类别、类别共有特征与真正体现发明点的区别性特征，再按第 7.7 节固定五类逐组确定性编译。申请人线采用“唯一申请人 + 客体”的专门门槛；其余四线分别只在自身要求的客体、核心发明点、分类号或功能效果事实足够时生成，不再套用旧的统一“三类任二”首轮组合。分类号既可以来自模块一著录事实，也可以由模型在通读专利后作为检索建议提出，但必须标明来源；模型推断分类号不得冒充著录事实、日期事实或法律证据。类别共有特征只承担召回和场景约束作用，不能因为对比文件具有该特征就被表述为破坏新颖性。模型负责形成冻结画像，固定五线及供应商字段表达式由确定性编译器生成；事实不足的组跳过并留痕，只有输入本身不足、模型/服务不可用或用户取消时才允许整项明确失败。
2. **逐篇新颖性检查**：每篇新文献先单独经过日期资格和 `I4-S` 比对。若一篇合格文献直接、明确地覆盖该权利要求全部限制，则该权利要求进入 `novelty_evidence_complete`，停止其默认后续检索并输出单文献新颖性 CC 表。
   每条实际执行的专利检索式返回的供应商原始前 10 篇候选必须全部冻结并进入 `I3-F`
   候选清理，不得用更小的隐藏抽样数静默截断。`I3-F` 先确定性合并同一申请、同一公开号
   不同公开版本及可靠同族重复，再用目标发明画像和候选题录做一次批量技术相关性复核；
   只有题名、摘要、分类号或其他元数据足以证明其属于明显无关技术领域且不包含可迁移的
   机构关系时才排除。没有摘要、分类号不足或语义存在疑问时一律保留。清理后所有唯一代表
   候选必须进入取文队列；被排除项和重复项仍逐条显示原排序、合并对象和理由。
   每篇成功取得真实全文
   的候选都必须分别执行 I4-S；未取得全文或模型分析最终失败的候选必须逐篇显示实际状态和
   原因，不能因至少一篇成功就把本模块整体显示为全部完成。
   I4-S 中，完整文献经逐特征审阅后明确没有公开某项技术特征时，状态必须是
   `not_disclosed`（未披露）；只有全文/附图不完整、定位不足、语义两可、证据冲突或分析
   失败时才使用 `uncertain` / `analysis_failed`。理由写明“未披露/没有公开”却输出
   “暂不能确认”属于状态与理由矛盾，必须由结构化输出约束和确定性一致性校验纠正。
3. **选择最接近现有技术**：如果没有单篇完整覆盖，则从合格文献中先按已确认相同技术特征数量选择覆盖最多者；并列时再按技术领域/用途、技术作用、所需结构改动、证据完整度和日期资格排序。文本向量相似度只能用于初筛，不能单独决定选择结果；系统允许人工改选并保存理由。
4. **形成并解释区别特征集合**：D1 未直接明确或必然隐含公开的目标特征均进入区别特征集合；每项必须保留目标原文、内容边界、在整体方案中的结构角色/连接位置/运动或控制关系、技术作用和 D1 未公开依据。`uncertain` 只能作为证据缺口，不能冒充已确认区别特征或已覆盖。
5. **显而易见性预分析**：`I4-O` 在 D1 和区别特征冻结后、任何 gap 查询生成前运行。对每项区别特征或相互配合的特征组分别分析客观技术问题、D1 内部具体启示及原文锚点、是否属于惯用技术手段/公知常识、改造动机与实现路径、反向教导以及技术效果是否可预期。D1 未直接公开的区别特征仍保持区别特征身份；但若“D1 已给出具体启示”或“该差异本身属于本领域惯用技术手段/公知常识”任一路径成立，并且存在具体改造动机与可执行路径、没有反向教导且效果可预期，则不再进行完全相同结构的普通 gap 检索。模型可以把惯用手段标为预判候选并据此抑制冗余结构检索，但证据完整结论仍须可引用证据或持久化律师确认。
6. **按预分析结果补证**：`I2-G` 只消费 `I4-O` 未解决的检索目标。D1 启示与惯用手段两条路径均不成立时检索直接特征证据；惯用手段仅属预判候选时只检索公知常识证据；累计特征线索存在但组合动机、启示或效果证据不足时只检索组合证据。已经通过任一显而易见性路径且满足动机、无反向教导和效果可预期条件的区别特征禁止重新进行完全相同结构检索。当前语料或律师确认已补齐全部目标时，不再生成查询，直接进入最终组合分析。
7. **进一步检索与补证循环**：首次进入模块九并取得已冻结的 D1、区别特征集合和 `I4-O` 后，系统一次性生成、校验并冻结第 1—5 个 gap 轮的完整检索矩阵；不得等前一轮执行结束后才临时生成下一轮关键词。该矩阵必须绑定 D1 选择版本、目标限制集合版本与哈希、待解决 `feature_id` 集合、日期资格 revision、`I4-O` run/prompt/rule 以及 `I2-G` prompt/rule；任一绑定变化时，尚未执行的旧矩阵不得继续使用，须保留旧版本并生成新矩阵版本。真实 provider 检索仍按轮执行：一次操作最多执行一个已冻结轮次的“检索 → 候选清理 → 取得/复用全文 → I4-S 全特征比对 → 逐区别特征覆盖复核 → 必要时重选 D1 → 重新运行 I4-O”；新文献若单独覆盖全部限制，立即进入新颖性 CC；若已解决全部检索目标，立即停止新增检索并进入组合分析。每一个实际执行的 gap 轮，必须为每一项仍未由可引用现有技术披露或其他符合本节证据标准的路径关闭的区别技术特征，执行一条矩阵中绑定该 `feature_id` 的主检索线；公知常识、组合证据、抵触申请或非专利补证线属于附加通道，不得挤占该逐特征最低覆盖。`I4-O` 可以改变该特征本轮检索的证据目的，但仅有模型预判、`human_review` 路由或尚无可引用依据的“惯用手段”不得使该特征在本轮成为零查询。每轮 I4-S 完成后按特征分别复核：已找到可引用披露依据的特征单独关闭，矩阵中该特征的后续轮次标为 `skipped_feature_resolved`，不得执行；没有找到的特征进入下一轮，不得因本轮其他特征有结果而整体停止。某轮的命中、零命中或分析失败只能决定后续行是否执行及失败原因，不得在同一矩阵版本内静默改写后续冻结查询。

   五轮矩阵中的后续轮次不得原样重复前一轮检索式。系统在矩阵生成时即须完成跨轮对账，并在仍保持目标专利事实可追溯和禁止从盲测答案倒推的前提下按固定语义阶梯调整检索角度：第 1 个 gap 轮为“相邻产品类别 + 直接结构特征”；第 2 轮为“上下位产品类别切换 + 结构族同义词”，其中应优先选择有检索价值的上位产品/设备类别，但目标客体已经过宽、继续上位只会退化为裸泛词或无法形成合格双语客体组时，必须选择与该区别特征用途和机构角色相符的具体下位产品类别，并保存选择下位类别的理由，不得因无法继续上位而让本轮失败；第 3 轮为“同功能产品类别 + 动作/功能/技术角色”；第 4 轮为“子系统/部件类别 + 构件关系/运动或控制关系/介质能量路径”；第 5 轮为“类比领域客体 + 工作原理/技术效果/等效机制”。每一轮仍须为矩阵生成时的每项未关闭区别特征恰好生成一组普通专利主检索式，并保持“双语客体 OR 组 AND 双语特征 OR 组”结构；执行时仅发送仍未关闭特征对应的行。模型只能在本轮语义视角内提供候选词，确定性编译器不得在本轮词族不足时退回上一轮词池换序或换一个成员冒充语义改写；无法形成本轮双语词组时必须明确失败或启动仅补齐本轮词汇的受控任务。扩展词必须与原具体词在同一可审计概念谱系中保存，不能退化成与目标技术无关的裸宽词。初始首轮不计入这 5 个 gap 轮；第 5 次后仍未解决时，以 `gap_search_exhausted` 收口并继续形成当前证据下的组合分析和大 CC 表，不得把“未找到”写成专利具备创造性。

   模块九每轮的普通专利主检索组合数量必须等于该轮仍未解决的区别特征数量，每项区别特征
   恰好对应一组并绑定唯一 `feature_id`。每组固定采用两个概念 OR 组做 AND：第一组是与目标
   客体接近但不使用目标专利完全相同类别名称的客体类别词，必须从目标客体的上位类别、下位
   类别或同一上位类别下的邻近产品类别中选择，并尽量同时包含中文、英文及可追溯同义表达；
   第二组是该区别特征对应的特征点词，必须包含其中文、英文以及可追溯的上位/下位概念或同义
   表达。结构固定为 `（邻近客体中文 OR 英文 OR 上下位/同义词） AND （特征点中文 OR 英文 OR
   上下位/同义词）`，不得把完整权利要求句、目标专利完全相同的客体名称或与该 feature 无关的
   附带构件塞入主线。例如目标为水枪且区别特征分别为吸水口、加压储水区域时，应形成两组：
   `（枪 OR gun）AND（吸水 OR 进水 OR 入水）`、`（枪 OR gun）AND（加压 OR 存水 OR 储水）`。
   若本轮新文献仍未提供合格披露，下一轮仍保持“一特征一组”，但必须在客体类别组或特征点组
   中实质更换至少一个检索角度；最多五轮。NPL、抵触申请、公知常识或组合证据可以作为附加
   通道，但不得在律师界面冒充新的区别特征组，也不得挤占上述逐特征普通专利主组合。

   “目标专利完全相同类别”只包括实际保护客体名称及其语义等同类别名，不得仅因画像同义词列表中包含
   上位类词就把该上位类词视为完全相同类别。模型客体组同时含完全相同类别词和合格上位/邻近类别词时，
   确定性层应移除完全相同类别词并保留审计；过滤后仍有中英文合格客体词即可继续编译，只有过滤后不再
   满足双语客体组时才 fail closed。该过滤不得放宽对目标实际客体名称进入可执行检索式的禁止。

   律师界面中每个 gap 轮是独立的持久化子批次。一次点击只等待当轮计划、检索、取文、日期
   核验和 I4-S 收口；当轮完成后进入 `awaiting_next_round`，只有用户显式继续才创建下一轮。
   存在计划、检索、候选清理、取文、日期资格或 I4-S 失败时，本轮进入 `round_partial` 或对应
   错误终态；已成功子任务不重跑，失败文献允许定向重试，未成功文献不得计入覆盖证据。任何
   单轮或多轮错误都不得阻断矩阵中后续尚未执行的轮次：系统必须保留本轮完整错误谱系和仍未解决
   特征，继续开放并在自动 Agent 流程中调度下一冻结轮次，直到五轮均已执行、因特征已解决而
   `skipped_feature_resolved`，或调查被用户明确取消。不得要求某一错误轮先变成成功才允许后续轮，
   也不得把带错误继续显示为该轮全部完成。页面的计划数、检索数、全文数、日期核验数和
   I4-S 数均按轮次分开统计，跨轮累计只能另行展示，禁止形成“累计 6 / 当轮 2”的分母错位。

   五轮检索矩阵须在模块九顶部一次性可见并默认折叠，逐轮显示 `pending`、`running`、`completed`、`partial` 或 `skipped_feature_resolved`，逐条文献显示 `new`、`reused`、`analyzed`、`failed`。后续轮命中与前轮相同文献时，仍须保存本轮 query、rank、round 和 provider 命中血缘；只有文献版本/内容哈希、目标限制版本/哈希、日期资格 revision、I4-S prompt/rule 和输入绑定全部一致时，才可复用既有 I4-S 单篇比对并记录 `reused_from_i4s_run_id`，不再重复取文和调用模型。若任一绑定变化则必须重取或重跑。即使 I4-S 可复用，只要 D1 选择或组合语料发生变化，依赖当前 D1/语料的 I4-O 与 I4-I 仍须重新运行，不得复用旧组合结论。
8. **创造性组合检查与模块十一三部分报告**：当没有单篇完整覆盖时，基于最新 `I4-O` 和模块九补证结果按三步法评估 D1+D2/D3、D1+公知常识或 D1 内部启示+惯用替换路径。第一步固定最接近现有技术 D1；第二步以 D1 的当前版本 `I4-S` 结果逐项固定区别特征及实际解决的技术问题；第三步遍历模块九各轮取得且通过创造性日期资格、全文和当前版本 `I4-S` 守门的补充语料，按每项区别特征选择有可核查披露增量的 D2/D3 或公知常识证据，并逐项核验相同结构角色/作用、明确技术启示或修改动机与具体修改路径、反向教导以及技术效果可预期性。模块九任一轮乃至全部五轮执行错误、零命中或未取得新对比文件，都不得阻止模块十运行；模块十必须以首轮、D1、既有日期合格且已完成当前版本 I4-S 的语料和当时所有可用事实形成降级三步法输出，同时逐轮列明模块九错误阶段、错误代码/安全摘要、受影响特征和证据局限。没有新增可用文献时不得伪造 D2/D3，结论必须明确为证据不足。多篇材料合计覆盖全部特征只是必要条件；只有每项区别特征均由合格证据补齐，且至少形成一条覆盖全部区别特征的完整结合路径，才可输出“现有证据已形成缺乏创造性的完整证据链（供律师复核）”。任一特征未公开、作用不对应、缺少启示/动机/路径、存在未排除的反向教导或效果不可预期时，必须输出“现有证据尚不足以证明不具备创造性”，并列明具体未闭合项；不得把它改写成专利确定具有创造性或有效。模块十一第一部分据此生成不增添事实的律师可读文字分析；第二部分从当前单次运行全部 `I4-S` 中按已确认披露特征数量、覆盖比例和证据完整度选取最多 10 篇，以全部目标特征为行形成横向状态矩阵；第三部分保留全部已逐篇分析对比文件的完整 CC。循环未解决全部 gap 或模块九存在部分/全部执行错误时，也必须如实输出当前文字分析、前十矩阵、全部逐篇矩阵、预分析状态、未解决项和模块九错误谱系。

正式 D1 只能在候选真实内容已经取得、按该权利要求完成日期资格和 `I4-S` 全限制比对后选择。检索引擎给出的首位或相似度最高结果只能叫“候选 D1”，不能提前写成最接近现有技术。若同一份合格材料已经完整披露全部限制，应优先进入单文献新颖性分支，而不是为了维持 D1/D2 结构继续组合。

D2/D3 也不是第二、第三个搜索命中，而是后续轮次中对当前未解决特征或组合理由提供最大有效增量的合格材料。D1 与 D2/D3 的累计覆盖只进入创造性分析；不同文件的累计披露永远不能被写成新颖性完整覆盖。

最接近现有技术不是第一轮相似度最高文献的永久标签。每轮取得的新文献都可以触发重新选择，形成新的版本；旧选择、替换理由、覆盖矩阵和由此重算的 gap 必须保留。相似度只能用于候选排序，不能代替日期资格、技术领域、用途/效果、特征披露、所需结构改动和证据完整度判断。

gap 至少区分：

- `feature_gap`：技术特征未披露；
- `evidence_gap`：疑似披露但缺少真实全文、原文定位、附图、译文或来源链；
- `date_gap`：公开时间、申请/优先权或公众可得性尚未核验；
- `combination_gap`：多份材料能够累计覆盖，但组合动机、技术启示、公知常识依据、技术问题或可预期效果不足。

区别特征循环还必须逐轮保存 `gap_search_iteration`（1—5）、当前 `obviousness_precheck` 版本、`gap_decision`（`skip_structural_gap_search` / `search_common_knowledge_evidence` / `search_combination_evidence` / `search_direct_feature_evidence` / `human_review` / `covered` / `exhausted`）、待检索与已解决的区别特征/特征组、D1 启示与惯用手段依据、反向教导与效果判断、复用的文献/I4-S run、生成的查询及上一轮失败原因。全文、单文献比对和预分析只有在文献版本、目标权利要求特征版本、内容哈希、日期资格、D1 版本和规则版本均一致时才允许复用；否则必须重取或重跑，不得以缓存命中掩盖版本变化。

第二/第三轮的触发目标必须记录具体 gap 类型。技术特征已经累计覆盖但仍缺少组合理由时，下一轮应搜索组合证据，不得继续把“找到更多相似文献”当作进展。

停止按独立权利要求生效：某一独立权利要求的新颖性证据完整，不代表其他独立权利要求都已解决。当前阶段不自动分析从属权利要求；未来如需恢复，必须由用户明确决定、升级宪章和工作流版本，并按继承限制加新增限制重新设计验收。

若模块一没有识别到任何独立权利要求，调查必须明确失败并提示修正解析结果；禁止回退为分析全部权利要求。历史版本已经创建的从属权利要求调查保持不可变，但新版汇总和律师页面必须将其标为“旧版范围外记录”，不得让它们改变当前范围内的状态或人工确认计数。

MVP 的自动区别特征循环必须有可配置上限，默认且公开契约硬上限为首轮之后最多 5 次 gap 检索循环；初始首轮不计入该数字。这只是资源和防止死循环的产品上限，不是法律限制。达到第 5 次后系统自动进入组合分析和大 CC 表，不再自动创建第 6 次 gap 检索；用户如需补充材料，应通过人工导入或新的显式调查继续，系统不得静默扩大本次循环上限。

### 4.7 模型能力边界

- 需要理解附图、扫描页面、结构图或图文对应关系的任务，必须使用经配置并验证具有视觉能力的 GLM-4.6V 系列多模态模型。
- 视觉模型不可用时必须明确失败或等待；禁止静默降级为纯文本模型。
- 模型负责检索辅助、特征对齐和证据候选解释；日期计算、资格过滤、唯一性约束和状态机由程序规则处理。
- 所有模型输出必须通过结构化 schema 校验，并保留模型、提示词和规则版本。

### 4.8 可重放、可审计和隔离

- 重新运行创建新的 run，不覆盖历史结果。
- 外部调用采用异步持久化任务，支持分页、断点、超时、重试、取消和限额。
- 模块实验室和 Agent 必须访问各自业务的同一权威后端。测试页面可以拥有逐模块、fixture 和技术审计能力，
  但不得因此切换到另一套服务、schema、token、上传根或规则版本；唯一后端配置缺失时必须失败，禁止回落历史 5109 release 或另一业务后端。
- 正式 Agent 是普通用户的唯一无效分析进度与结果视图。工具卡必须在对话内直接给出可理解的当前
  事项、结果摘要、证据边界和必要确认；普通用户不得再被“查看分析进度与结果”引导到技术型
  `/invalidity/results` 页面。历史结果深链可以保留为兼容入口，但不得成为新的普通用户导航。
- `/test/module-lab` 和 `/test/product-pipeline` 分别是无效分析、侵权分析的开发管理员同任务诊断
  视图。它们必须能够按 Agent 的 `analysis_session_id` 解析同一后台任务，读取同一持久化状态、
  模块输入输出、轮次/阶段和错误；仅为查看进度和问题不得复制调查、创建第二个任务、重跑模块或
  改写原任务。只有管理员可以看到 Agent 入口并访问带 session 的只读诊断模式；退出只读模式后需要
  主动重跑某模块时，仍须由管理员显式操作并产生新的可审计 run。
- 其他 `/test` 调试页面同样必须鉴权。律师/开发管理员使用的 `/test/module-lab` 只提供“当前真实案件”和“内置示例”两种输入方式；mock、manual、原始 JSON 和 provider 选择只允许保留在高级技术页面或底层测试接口，不得成为律师测试业务模块的必填步骤。
- 密钥、鉴权头和敏感原始响应不得写入前端、普通日志或导出报告。

### 4.9 轮次状态和检索谱系

每次迭代至少保存：

- `claim_investigation_id`、目标权利要求和完整继承文本；
- `iteration_no`、`parent_iteration_id`、检索目的和触发原因；
- 本轮目标 `gap_feature_ids`、锚定文献和使用的查询/分类/引证路径；
- 新发现、重复、日期不合格、仅作线索和进入比对的文献；
- 单文献覆盖矩阵、最接近现有技术选择及其历史版本；
- 创造性组合候选、仍未覆盖特征、组合理由缺口和人工决定；
- 本轮耗时、成本、provider、模型/规则版本和停止原因。

每个 iteration、query、module run 和 job 都必须进入明确终态并写入完成时间或失败原因；不得留下调查已经结束但轮次仍为 `running`、查询仍为 `planned` 的虚假状态。每轮提交是原子审计边界：只有该轮的检索、日期资格、单文献比对、D1/gap/组合结果和状态均持久化后，`I0` 才能创建下一轮。

权利要求调查状态至少区分：`initial_search`、`single_reference_review`、`novelty_evidence_complete`、`closest_prior_art_selected`、`obviousness_precheck`、`gap_search`、`combination_review`、`inventive_step_evidence_complete`、`needs_human_review`、`search_budget_exhausted`、`partial` 和 `failed`。

## 5. 当下必须避开的依赖库和依赖方式

以下限制适用于无效检索 MVP 的新增依赖，不要求删除现有系统中与本功能无关且已验证的依赖。这里限制的是不可靠、不合规和不可审计的使用方式，不对第三方库、Google Patents 或浏览器自动化作一刀切禁止。

### 5.1 明确允许评估和使用的来源与工具

在能够取得真实对比文件或真实公开内容的前提下，允许：

1. **第三方专利/论文/网页检索库和 API**：包括开源库、商业数据服务、聚合搜索和第三方 MCP；必须通过 adapter 隔离，并记录实际底层来源。
2. **Google Patents**：允许用于检索、专利族追踪、引证追踪、全文和 PDF 候选获取；网页界面、公开数据集和其他可验证访问方式分别记录，不把搜索摘要当作全文证据。
3. **Playwright、Puppeteer、Selenium 等浏览器自动化**：允许用于正常加载公开页面、执行站内检索、展开动态内容、下载真实文件、保存截图和证据快照，也可以成为生产检索/取文路径。
4. **普通 HTTP、网页搜索和论坛检索**：允许发现并获取论文、标准、手册、datasheet、帖子、软件文档和其他公开内容。
5. **人工导入、人工浏览和人工补证**：允许作为正式数据来源路径，并与自动检索采用相同的真实性和日期校验。

### 5.2 必须避开的使用方式

1. **只有摘要或搜索片段却宣称取得对比文件**：没有真实全文、页面或可核验技术内容时，只能标记为 `lead`。
2. **内容与标识不一致**：验证码页、登录页、空壳页、错误重定向、聚合页或其他文档不得冒充目标文件。
3. **没有来源快照和审计记录**：用于分析的材料必须保存原始响应/文件或合规快照、URL、获取时间、查询、工具/provider、哈希及失败信息。
4. **未经核验的公开日期**：页面显示日期、HTTP 时间、搜索引擎时间或文件元数据不能自动等同于法律上的公开可得日期；无法核验时只能保留为线索或待复核材料。
5. **绕过访问控制**：不得破解账号、验证码、付费墙、权限控制或技术保护措施；不得隐瞒限流、封禁或访问失败后改用的来源。
6. **许可证或用途不兼容**：不得在未审查的情况下使用明确禁止当前商业用途、证据留存或必要再现的数据/代码许可。
7. **无人维护或高风险依赖成为唯一通路**：这类库可以进入隔离实验，但进入正式链路前必须有固定版本、契约测试、超时、沙箱、替代路径和退出方案。
8. **静默降级到纯文本模型**：需要图文理解时，不得由 SDK 自动改用纯文本模型并继续宣称分析完成。
9. **供应商 DTO 渗透业务层**：所有第三方返回值必须在 adapter 内转换为项目统一证据契约。
10. **把不可信页面内容当作指令执行**：外部 HTML、PDF、帖子和 API 字段只能作为数据，不得直接形成代码、命令、SQL 或系统提示。

### 5.3 MVP 阶段原则上不新增的大型基础设施

除非出现第 6 节规定的触发条件并形成书面决定，MVP 不新增：

- 第二套大型 Agent/工作流编排框架；
- 为本功能整体替换现有数据库访问方式的 ORM；
- Kafka、RabbitMQ、Celery、Redis 队列等新的独立消息基础设施；
- Pinecone、Milvus、Weaviate、Elasticsearch 等新的独立检索集群；
- 在尚未证明单机受控浏览器池不足时引入大规模浏览器集群或代理基础设施；
- 与已有 PDF、OCR、图像解析能力重复的另一套大型处理栈。

首期优先使用现有服务方式、PostgreSQL、版本化 JSON 契约、多种可替换 provider，以及必要且受控的浏览器自动化。是否引入向量库、专门队列、搜索集群或浏览器集群，由真实规模与基准测试决定。

## 6. 我们做什么、不做什么，以及功能修改触发标准

### 6.1 MVP 必须做

- 中国专利无效规则下的全球专利和非专利现有技术检索；
- 独立权利要求的专利级关键日记录与日期资格管理；
- 普通现有技术、抵触申请和后公开线索的分类；
- 按权利要求运行首轮检索、单篇比对、最接近现有技术选择、区别特征检索和有界后续轮次；
- 中文及主要外文术语、IPC/CPC 和引证追溯检索；
- 专利、论文、标准、手册、datasheet、论坛/网页及人工材料的统一证据模型；
- 新颖性单篇比对、创造性组合分析；
- 原文定位、来源、公开时间依据和内容哈希；
- CC 表、创造性组合表、排除记录、检索范围和未解决证据缺口；
- 可独立调试的模块实验室；
- 与侵权流程的严格业务隔离，以及不同用户、会话和运行版本的严格数据隔离。

### 6.2 MVP 明确不做

- 不宣称检索已经穷尽全球所有现有技术；
- 不自动作出“专利有效”或“专利无效”的最终法律结论；
- 不自动提交无效宣告请求或代替专业人员签署法律文件；
- 不把无法证明公开时间的论坛、网页、产品资料直接当作合格现有技术；
- 不绕过登录、付费墙、验证码、robots、访问频率或数据许可限制；
- 不使用关键日后的普通材料进行创造性组合；
- 不用多份文献拼接新颖性；
- 不以单一、不透明的稳定性百分比分数替代证据和逐项分析；
- 不在实现无效检索时顺便重写现有侵权业务、认证系统或整个前端；
- 不在没有数据源实测前承诺某个平台具备完整全球覆盖、全文、附图或法律状态。

### 6.3 功能修改的允许触发标准

只有满足至少一项以下条件，才允许修改产品功能或核心实现：

1. 对应本文件列明的 MVP 必须功能，并且可以给出明确验收证据。
2. 修复法律日期规则、证据资格、新颖性/创造性边界等实质正确性问题。
3. 修复跨会话、跨业务模式或同一业务多后端分叉造成的数据、规则或端口污染。
4. 修复证据无法追溯、结果不可重放、任务状态虚假完成或错误被吞掉的问题。
5. 修复权限、密钥、用户数据、供应商授权或日志泄露风险。
6. 有测试或运行数据证明当前实现无法达到已经确认的正确性、覆盖率、稳定性、性能或成本目标。

纯粹因为“可能以后有用”、代码风格偏好、追逐新框架或未经验证的性能猜测，不构成功能修改触发条件。

### 6.4 必须先更新本文件的变更

以下变更在实施前必须先修改本文件、记录理由并取得用户明确批准：

- 改变中国法域或增加其他法域的法律结论；
- 改变关键日、抵触申请、新颖性或创造性的规则；
- 增减 MVP 数据源类别或把实验数据源提升为正式证据源；
- 新增受限许可证、商业数据源或新的常驻基础设施；
- 改变 GLM-4.6V 多模态必需条件或允许纯文本降级；
- 合并侵权与无效结果表、重新引入同一业务的正式/测试双后端或改变用户数据边界；
- 突破本文件第 5 节规定的证据、依赖或访问控制边界；
- 改变“不作最终法律结论”的产品边界。

### 6.5 新依赖和新数据源的准入检查

新增依赖或 provider 必须同时证明：

- 现有能力无法合理实现该需求；
- 有明确维护状态、版本策略和许可证；
- 允许当前商业和证据留存用途；
- 能提供所需字段、公开日期和稳定标识；
- 有限流、成本、失败和退出方案；
- 不要求测试环境调用正式凭据；
- 有契约测试、固定样本和回滚方式；
- 不会把不可信外部内容直接执行为代码、SQL、命令或系统提示。

## 7. 开发前必须先做的数据源验证

在开始无效检索业务代码之前，先完成一轮小规模、可记录的数据源验证。它不是为了只选官方 API，而是为了确认哪些方式真的能找到并取得可用对比文件。

### 7.1 验证对象

同场评估：

- EPO、WIPO、CNIPA、USPTO 等官方数据/API/检索入口；
- Lens 等第三方或商业 API；
- Google Patents 网页和公开数据集；
- 可用的专利 MCP 和第三方库；
- Playwright/普通 HTTP 对公开全文、PDF、附图和动态页面的实际获取；
- 论文、标准、产品手册、datasheet、论坛/网页等非专利来源；
- 人工导入和人工补证路径。

### 7.2 验证样本和指标

- 使用一组覆盖 CN、WO、EP、US、JP、KR 等来源的已知专利，并加入论文、标准、datasheet、产品手册和论坛材料样本。
- 对相同主题、关键词、IPC/CPC、申请人和引证追溯任务进行横向比较。
- 记录是否找到真实文件、全文/权利要求/附图完整度、日期字段、专利族、引证、稳定 URL、页面真实性、延迟、费用、限流、验证码和失败恢复。
- 对非专利文献额外记录公开时间证明强度、内容是否可快照、动态页面变化和是否需要人工补证。
- 至少选择一项具有已知对比文件链的目标权利要求，完整验证“首轮检索 → 单篇比对 → 最接近现有技术 → gap 查询 → 第二/第三轮检索 → 新颖性或创造性输出”，而不是只验证一次关键词搜索。
- 记录每轮新增有效文献数、重复率、gap 是否缩小、最接近现有技术是否变化、是否出现无进展循环，以及同一查询在不同 provider 的差异。

### 7.3 验证输出和通过标准

验证结束必须形成：

1. provider/source 能力矩阵；
2. 专利和非专利材料的主检索、补充检索、取文和人工复核路径；
3. `lead -> retrieved_document -> qualified_evidence` 的升级规则；
4. 每种来源的成本、限流、失败、合规和替代方案；
5. 可用于后续模块契约测试的固定真实样本和脱敏 fixture；
6. 明确的 go/no-go 结论：哪些来源进入 MVP 正式链路，哪些保留为实验或人工入口。
7. 一份可重放的多轮检索样例，包括每轮查询谱系、文献快照、覆盖矩阵、最接近现有技术选择、gap 变化和停止原因。

这一步可以使用现有浏览器、API、命令行和人工操作进行验证。用户已于 2026-07-19 明确授权开始系统开发；仍应先取得数据源验证的实际证据，再冻结正式 provider 和契约，不得用未经验证的来源冒充已完成检索能力。

### 7.4 2026-07-20 专利 provider 验证结论与准入顺序

本机真实网络验证中，Google Patents XHR 与详情页均出现 `503`，Brave 公开网页出现
`429`/访问验证页；Yahoo 公开结果页能够返回指向 Google Patents 的真实链接，但不能替代
真实文献获取或证明全球检索完整性。因此冻结以下边界：

1. **智慧芽（Patsnap）是当前用户指定的主 provider 候选**：用于全球专利候选发现、书目、
   日期、专利族、全文/图片及其 API 实际提供的其他能力。只有在取得接口文档、测试凭据、
   商业/证据留存许可说明，并通过真实样本连通性、字段完整性、限流、日期双通道和文件哈希
   验证后，才可标记为正式可用。智慧芽返回结果在验证前只能按统一证据阶段处理。
2. **EPO OPS 暂时停用，USPTO 暂不作为主检索源**：已有 adapter、fixture 和配置保留，
   但在用户另行要求前不作为当前 live 默认 provider，也不得与智慧芽凭据交叉使用。
3. **Google Patents 网页/XHR不是受支持的稳定 API**：仍可作为实验发现或取文路径，
   但限流、验证页或结构漂移必须显式失败。不得通过 CAPTCHA 绕过、无限重试或伪装浏览器
   来制造成功。
4. **Yahoo 等公开搜索页只产生 `lead`**：其查询、响应哈希、时间、实际 provider 和退化
   范围必须留痕；该批次必须标记为 `partial`，搜索摘要不得进入披露矩阵或 CC 表。
5. **WIPO、USPTO 及相应国家/地区局用于官方文件补取和核验**；其覆盖范围不足时允许人工
   导入。Serper、Brave Search API 或 Lens 只有在凭据、商业用途、结果留存和许可审查通过
   后，才能作为额外发现源。
6. provider 必须声明 `discovery`、`bibliographic_verification`、`full_text`、`pdf`、
   `images`、`family` 和日期字段能力；工作流不能因某一 provider 找到链接就推定其他能力
   已完成。
6. 在 EPO OPS 或另一条经真实验证的全球专利路径通过完整 live 样本前，MVP 不满足第 8 节
   第 3 项，实例回归也不得把网页 fallback 的结果写成完整成功。

### 7.5 2026-07-23 智慧芽作为专利检索默认通路

用户进一步确认：当前及后续无效检索中的自动化**专利候选发现**统一优先使用智慧芽
（Patsnap）。测试版和未来显式 promotion 的正式版均不得静默改用 USPTO、EPO、Google
Patents 或网页搜索；只有用户另行明确改变 provider 决定时才能切换。该决定只约束专利
检索，不把智慧芽扩张为论文、标准、手册、datasheet、论坛等非专利文献来源。

本节第 2 至第 5 项记录 P002 初始接入约束；首轮查询组合、执行数量和零命中处理已由
2.15 / 第 7.7 节取代。固定五线不得适用下述 `compact-fallback` 历史规则。

当前账号只有 P002、P060 beta、P061 已通过真实权限验证，因此边界仍为：

1. P002 返回的题录只能作为 `lead`；P012/P018/P019、真实全文、PDF、附图和日期来源链未
   验证前，不得升级为 D1、`retrieved_document`、`qualified_evidence` 或 CC 表依据。
2. 面向 P002 的首轮文字查询必须形成可审计的字段化查询组合，而不是固定要求“技术主题 +
   两个必要特征”。至少包括：
   - 一条发明点精确线：优先以分类号或保护客体/类别作为锚点，加能够说明发明点的区别性
     特征，通常在说明书全文检索；若该区别性特征预计会写入摘要或权利要求，应选择对应
     字段并记录理由；
   - 一条权利要求语境线：优先使用保护客体/类别同义词，加用于识别同类技术方案的
     结构/功能语境组，在权利要求字段检索。语境组可以是类别共有特征，但不得被标为
     发明点或据此直接评价新颖性；
   - 条件允许时增加标题摘要线，用于召回在发明名称或摘要中直接突出发明点的文献。
   上述角色决定检索目的和字段范围，但硬守门统一采用“三类任二”：技术特征关键词、
   保护客体/类别、分类号中任意两类实际出现在可执行式中即可，三类同时存在也可。
   分类号可以是已解析事实或带来源标记的模型检索建议；后者只用于检索，不得回写为专利
   著录事实或证据。只命中一类的孤立宽词仍应拒绝。
   I2 第一次生成不合规时必须把拒绝原因反馈给生成器并继续生成，不能把单次模型格式或
   组合失败直接交给律师；连续模型失败时可以用已冻结客体、真实特征词和分类建议进行
   确定性组装，所有尝试均保留审计。
3. 每个客体、发明点和语境组在模型确实提供对应术语时，都必须平衡保留中文和英文；不得因固定数量
   截断而把某一语言全部排除。英文多词术语应保留可检索的短语/紧凑术语，不得机械删除
   `and`、`with`、连字符后形成失真的表达。
4. 单条合规 P002 查询返回零命中时，不得直接把一次查询的 `0` 当作本轮检索结论。系统应
   继续执行同一首轮组合中尚未执行的其他字段/目标查询；每条查询都有独立 run、独立幂等键
   和完整审计。某一查询需要紧凑回退时，至多创建一条保持原客体/分类锚点、原检索目标和
   原字段范围的紧凑双语查询，不能退化为孤立词。全部计划查询均零命中时才显示需要人工
   调整或进入下一轮，不得伪造候选或宣称没有现有技术。
5. 每个 P002 run 的 POST 仍只允许一次实际 attempt；所谓第二条查询是可审计的新查询，不是
   对未知结果的传输重试。

### 7.6 2026-07-27 智慧芽发现、EPO OPS 优先取文、P020 同文献补取

用户进一步明确专利侧 provider 分工：

1. 智慧芽 P002 继续是自动化专利候选发现的唯一默认通路，EPO OPS 不接收 I2 检索式，
   不作为 P002 零命中或失败时的候选搜索回退。
2. 智慧芽 lead 返回公开号、申请号或其他可规范化专利标识后，I3-E 应优先使用公开号
   请求 EPO OPS；只有公开号缺失时才允许使用申请号解析对应 publication reference。
3. EPO OPS 负责候选身份核验及其实际覆盖范围内的书目、公开日期、全文页面、PDF 或图片
   补取。EPO 返回标识必须与智慧芽 lead 规范化后精确对应；仅同族相似、题名相似或检索
   命中不能替代同一文献身份校验。
4. EPO access token、Consumer Key/Secret 和鉴权头只存在于当前环境服务内存或权限为
   600 的环境文件，不得进入源码、数据库、前端、普通日志、报告或工件。测试/正式必须
   分别使用本环境前缀变量和隔离配置文件；经用户或运维人员显式授权，可以把同一组外部
   provider 凭据值分别写入两份配置，但运行时不得读取另一环境变量、跨环境回落或动态借用。
   当前环境缺少自身变量时必须 fail closed。
5. EPO 元数据、全文地址或页面目录仍只说明“发现了官方取文路径”。只有实际文件/页面
   下载、MIME 与文献身份校验、原始字节冻结和 SHA-256 完成后才能升级为
   `retrieved_document`；公开日期和 I4-S 技术披露仍按原规则分别核验。
6. EPO 对同一公开号明确返回无覆盖、无全文能力或没有可下载文件/页面，且请求已正常完成、
   身份没有冲突时，I3-E 才调用智慧芽 P020，以 P002 已冻结的同一 `patent_id + pn` 补取
   PDF。该调用是取文 fallback，不得扩大或替换 P002 候选集合，也不得改用同族、相似题名
   或其他公开号。
7. “EPO 没有”必须是可审计的 `not_found`、`no_coverage` 或 `media_unavailable` 终态。
   EPO 鉴权失败、网络/超时、限流、服务异常、响应无法解析、申请号多义或文献身份不一致
   不得触发 P020；这些情况必须保留为 EPO 失败或人工复核，不能被 fallback 掩盖。
8. P020 返回的下载地址仍只是临时取文线索。只有实际下载的字节通过 PDF 文件签名、MIME、
   大小和 `patent_id + pn` 精确身份校验，并冻结原始 PDF、metadata 响应、逐次调用审计和
   SHA-256 后，才能升级为 `retrieved_document`。P020 不可用或校验失败时，候选保持
   `lead` / `evidence_gap`，不得伪造全文。
9. 文献 provenance 必须分别保存 `discovery_provider=patsnap`、按顺序发生的取文尝试
   `epo_ops → patsnap_p020`、每次尝试的终态以及最终选中的 `retrieval_provider`。P020
   的成功不得覆盖或改写前一次 EPO 无覆盖记录。

### 7.7 2026-07-29 通用机构检索、可读全文与结构披露标准

1. I2 生产查询只能读取目标专利冻结事实、当前独立权利要求及其既有轮次事实。决定书、已知
   对比文件、公开号、题名、摘要和从中人工摘出的词只能存在于测试评估器的隔离答案清单，
   不能进入模型 prompt、查询编译器、同义词表或排序规则。
2. 首轮检索以目标专利的保护客体和机构工作模型为起点。模型提取部件角色、部件间关系、
   运动/状态变化、控制/触发关系和技术作用，并生成原文表达、同角色表达及运行关系表达。
   I2 必须形成多分辨率检索组合，而不是只选一条“最佳”表达：
   - `maximal_similarity_precision`：保护客体加多个高区分度发明点/机构关系，组间 AND；
     允许四个以上概念组，但不得机械复制完整权利要求或加入类别共有低信息词。该线用于寻找
     尽可能相似的文献，零命中是允许且有信息量的结果，不触发把原式静默裁短的同线回退；
   - `object_plus_inventive_point`：保护客体加一个发明点；
   - `subject_classification_plus_inventive_point`：保护客体所属分类号加一个发明点；
   - `object_plus_inventive_classification`：保护客体加发明点/机构所属分类号；
   - `object_plus_two_inventive_points`：保护客体加两个彼此不同的发明点/机构关系。
   短线通常保持二至三个文字概念组，组内 OR、组间 AND；分类号作为独立字段约束。若分类号
   不能可靠区分“客体分类”和“发明点/机构分类”，必须标为 `general/model_suggested`，
   不得伪造分类角色；此时应对目标专利已有的分类号作有界逐条分线，不能武断只取第一个。
   短线应同时保存目标原词和与其结构/动作含义相同的作用等价词组，但可执行检索式的主锚点
   必须优先使用目标权利要求原词或可回溯的发明点短语；等价词只能作为同一原词旁的受控 OR
   扩展，不能替代原词单独成为检索锚点。联接/接合、锁定、解锁、脱开/分离属于不同机构
   动作；目标只记载选择性联接时，不得由程序或模型自行扩展为锁定机构、解锁机构或锁定/
   解锁动作。任何等价词必须由目标权利要求/说明书的同一部件角色、结构关系和动作机理推出
   并绑定 feature ID，不能从已知对比文件倒推。供应商编译时不得用单一宽词替换完整客体或
   具体特征，但应在同一 OR 组中同时保留可回溯的具体词与核心含义词/合理上位词，以避免多个
   过细 AND 组共同造成零召回。上位词必须由目标专利名称、摘要、权利要求、说明书或模型对
   已冻结概念的通用化分析支持，并保持同一客体、部件角色、动作或介质路径；不得从已知对比
   文件倒推，也不得扩成与目标技术无关的裸词。通过上述来源守门的作用等价词组优先于自动
   生成的 `structure` 等无信息碎片词。
   可执行 feature anchor 必须是单一、可独立检索的技术概念，不得把说明性句子、关系描述
   或由顿号/逗号/分号并列的多个概念当作一个关键词。例如“中间位置、选择性联接”必须拆成
   “中间位置”和“选择性联接”两个关键词组；短召回线可以只使用更能说明发明点的
   “选择性联接”，确需位置限定时才以独立 AND 组加入“中间位置”。“位置选择性联接装置”
   这类本身表达一个完整技术概念的专利术语可以整体保留。不得为了保留模型原句而把不同
   特征塞进同一 OR 组或同一个字符串。
   可执行检索词还必须剥离专利标题、权利要求和说明书中的撰写外壳：`具有/具备/带有/设有/
   设置有/采用 + 技术概念 + 结构/装置/机构/系统/组件/部件/单元` 不是一个完整检索概念。
   发明画像和证据展示继续保留原始表述以便追溯，但查询编译时应提炼其核心动作、功能或部件
   含义，例如“具有升降结构”编译为特征词“升降”。当这类属性表述位于客体之前时，客体 OR
   组只能保留产品类别名词，例如“具有升降结构的室外消火栓”的客体为“室外消火栓”；“具有
   升降结构”不得成为客体同义词。提炼出的“升降”等核心词只能绑定到画像中已有的相应特征/
   feature ID，不得跨角色混入客体组，也不得由程序凭空增加目标专利未记载的技术含义。该规则
   对全部 I2 检索线、确定性组装、模块四 GLM 提案清洗及离线重编译统一适用，禁止案件词表。
   最大相似长线只使用冻结的完整锚点组，不作结构碎片扩展；零命中仍按设计正常收口。
   发明点自创术语改写：当发明点术语属于自创/低通用度命名（未在类别共有语境或通用构件层
   出现、除目标专利自定命名外无通用对应词）时，不得单独以该自创词作 AND 锚点承载首轮
   召回线。画像必须为每个发明点特征额外产出功能表达：上位通用构件词（如「阀/valve」）
   与功能效果词组及其中英文同义（如「迸射/水弹/shot/water bullet」，描述发明点实现了
   什么而不是叫什么名字），来源标记、≤6 字原子化、每词 OR ≤5 成员等既有守门照旧适用。
   首轮检索线混编：一部分线采用功能表达（客体与通用构件在权利要求层 AND、功能效果词组
   在说明书/全文层 AND，结构如 `CLMS:((水枪 or water gun) and (阀 or valve)) and
   DESC:(迸射 or 水弹 or Shot or water bullet)`），另一部分线保留原术语表达；自创词
   术语线可以存在但不得独占首轮位置。规则对任意专利通用，禁止案件特定词表。
   首轮固定五组检索线（自 2.15 起取代前述多分辨率首轮组合，其他线型不再首轮发射）：
   第一组（仅一组）客体加同专利权人其他专利：以目标专利申请人/专利权人字段检索其
   其他专利并叠加客体词，排除目标专利本身；仅在申请人唯一时生成，申请人缺失或有歧义
   时跳过该组并留痕。真实 P002 已验证 `AN` 字段有效，但供应商不接受 `NOT PN` 排除语法；
   因此公开号存在时，供应商只执行已验证的申请人+客体表达式并完整冻结原始排序，随后由
   本地确定性层按规范化公开号排除目标专利并记录排除项。公开号缺失时不得伪称已排除，
   必须在计划中留痕。
   第二组：标题检索客体及中英文同义词，AND 说明书检索核心发明点及中英文同义词。
   第三组：客体相关分类号约束，AND 说明书检索核心发明点及中英文同义词。
   第四组：说明书检索客体及中英文同义词，AND 说明书检索核心发明点及中英文同义词，
   AND 说明书检索技术效果词。
   第五组：标题和关键词检索客体及中英文同义词，AND 说明书检索核心发明点的功能描述
   及中英文同义词（即 2.14 的功能表达，承载自创术语改写要求）。
   各组术语仍只能来自画像冻结事实或模型对该冻结概念生成的可追溯核心含义/合理上位表达；
   每个客体、部件、动作和功能概念组应采用“具体原词/近义词 OR 核心含义词/上位词”的召回
   结构，例如具体客体可并列其核心产品类别，具体管路/吸水结构可并列“供水管/供水”“吸水
   结构/吸水”等同角色核心词。上位词只进入原概念的同一 OR 组，不得另建宽词检索线，也不得
   删除具体词；每个最终供应商 OR 组 ≤5 成员、≤6 字原子化、分类号来源标记等守门照旧；裁剪
   优先保留至少一个具体中文词、一个核心中文词及已有的跨语言对应词。首轮即此
   五组，不超过五条；事实不足的组跳过并留痕，不得补造。固定五线逐 `query_id` 独立执行，
   零命中是有效结果，不创建 `compact-fallback`，也不得静默裁短原式。规则对任意专利
   通用，禁止案件特定词表。
   固定五线的锚点充分性只在 I2 画像和查询编译阶段校验一次。某条查询已经属于同案、同独立
   权利要求的成功 I2 运行，具有固定五线 `query_variant`，并以 `query_id` 和冻结的
   `provider_expression` 精确回读时，I3 必须把五条（或事实足够形成的不足五条）作为一个已
   守门检索组合逐条真实执行；不得再用旧的通用单条检索守门把申请人线误判为“只有技术主题”，
   也不得因长发明点已经按本节规则原子化而要求原始长句逐字出现在供应商表达式中。浏览器临时
   拼接、跨案引用、非固定变体、表达式不一致或没有成功 I2 谱系的查询仍按原守门拒绝。
3. 检索范围按词语预计出现位置确定：客体和总括用途优先标题摘要，部件组合优先权利要求，
   位置、连接、运动和工作过程优先说明书全文。解析得到的分类号可作为约束；模型建议分类号
   只能形成有来源标记的探索线，不能冒充著录事实。
4. 已知案例只用于盲测：全部计划查询及其供应商原始前十结果冻结后，评估器再检查预期文献
   是否出现在任一检索组的前十或其去重后的待取文集合中。命中决定书对比文件 1 即视为该
   基准的首轮召回合格；未命中时只报告通用角色、表达、字段、分类来源或排名原因，不把
   答案用词反向写入生产规则。
   内置盲测必须跟随规则升版：模块六（I4-S）判断规则每次升版后，必须用当前规则对五个
   真实无效内置盲测案件的全部比对单元重新逐篇分析，原子冻结为带当期日期与规则版本的
   新版本目录，并把模块实验室前端白名单指向新冻结版本；内置盲测展示必须始终对应当前
   规则版本，禁止长期展示旧规则冻结结果。检索计划（I2）冻结只在检索规则升版时同样重跑。
5. I3-E 统一生成版本化可读文献。优先使用 EPO description/claims XML，次选 PDF 文本层，
   仅对无文本页面进行本地 OCR。原始文件取得与 `analysis_ready` 分开；OCR 在后台有界运行并
   按原始内容 SHA-256、标准化版本和 OCR 配置缓存。只有全文覆盖满足规则且锚点可回溯时，
   才允许 I4-S 输出 `not_disclosed`。
6. I4-S 的“公开”判断采用整体机构对应标准，而不是字面或零件名称逐项同一标准：术语、构件
   划分、零件数量和具体外形可以不同；只要对比文件中的一个构件或一组相连构件在整体方案中
   承担与目标特征实质相同的部件角色，并实现相应的介质/能量路径、连接拓扑、运动/状态变化
   和控制关系，即可认定结构披露。一个集成构件可以同时承接目标权利要求中多个分别命名的
   结构角色，不得仅因对比文件没有逐字写出每个目标名称而否定披露。

   对比文件没有明说目标名称时，还可以根据完整结构和工作过程认定 `necessarily_implicit`：
   必须逐步说明“已明确公开的构件与关系 → 运行所需的物理/机械条件 → 必然存在的目标角色或
   关系”，并排除与该文献记载相容的合理替代路径。例如，只有在整体机构表明储液腔参与受压
   排液时，`reservoir` 才可按其实际角色映射为压力罐；只有在单向阀的活塞/杆件于阀体流道中
   移动并控制通断时，才可把该集成结构映射为可移动阀杆和阀导管，而不能因出现
   `reservoir` 或 `check valve` 名称就无条件等同。只有功能、目的或效果相同，但结构角色、
   工作路径或必要关系仍无法确定时，才不足以确认公开。模型负责全文机构理解、角色映射和
   必然性链说明；确定性规则负责全文资格、证据锚点、状态一致性和禁止越界，系统仍不输出
   最终法律结论。

## 8. MVP 完成判定

只有在以下证据同时成立时，才能声称 MVP 完成：

1. 同一上传专利可以分别创建侵权和无效会话，且后续记录完全隔离。
2. 每项独立权利要求具有可解释的专利级关键日和日期依据；不确定项会阻止确定性结论。
3. 至少一个经验证的全球专利获取路径、一个非专利获取路径和人工导入路径通过契约测试；路径可以采用官方 API、第三方服务、Google Patents 或浏览器自动化，但必须满足统一证据标准。
4. 日期测试覆盖普通现有技术、抵触申请、关键日后不合格文献和日期未知文献。
5. 新颖性 CC 不拼接文献；创造性组合中每份材料均通过日期检查。
6. 每条实质分析都能定位到原始来源和具体内容位置。
7. 模块可以使用 fixture 独立运行，失败、部分完成和取消状态真实可见。
8. 两项业务的唯一后端配置缺失时均 fail-closed；Agent 与对应模块实验室解析到同一服务身份、规则版本和数据命名空间，不发生历史正式/测试端口或凭据回落。
9. 导出的 CC 表、组合表、排除记录和检索审计记录可以重放并对应具体版本。
10. 人工专业复核入口存在，系统没有输出无法由证据支持的最终法律结论。
11. 至少一个固定样例证明系统能按未覆盖特征继续第二/第三轮检索；每轮新文献都会先做单篇新颖性检查，再进入创造性组合。
12. 每项独立权利要求都具有独立停止状态；一个独立权利要求解决后不会错误终止其他独立权利要求；从属权利要求不会创建当前阶段调查或污染汇总状态。
13. 最接近现有技术的选择依据、人工改选和历史版本可重放；不存在仅凭文本相似度自动作出法律选择的情况。
14. 达到轮次/预算上限或连续无进展时，系统输出未解决 gap 和停止原因，不把检索未命中转换为有效性结论。
15. `/Users/xiexiaoxiong/Documents/Patent项目配套/实例专利` 中的全部专利文件均已被回归清单识别，并逐件跑完无效检索测试流程；失败会保存阶段、原因和重试记录，修复后重新运行，最终不存在遗漏、静默失败或虚假成功案例。
16. 全量实例回归完成后，Agent 与模块实验室共用后端、业务间及用户/会话数据隔离、前端模块调试入口、CC 输出和导出均通过与其风险相称的验证，并检查最终 Git diff 未覆盖既有用户修改。

## 9. 已确认的产品决定

- 2026-07-19：无效分析适用中国法律规则，但检索范围覆盖全球。
- 2026-07-19：新颖性与创造性均纳入首期分析范围，但采用独立规则和结果。
- 2026-07-19：现有技术范围不限于专利，包括论文、标准、产品手册、datasheet、论坛及其他可证明公开时间的材料。
- 2026-07-19：与现有侵权系统共用前端入口和中立专利解析能力，后续业务数据与运行结果隔离。
- 2026-07-19：在开始业务代码前，先建立并强制预读本项目宪章。
- 2026-07-19：不禁止第三方库、Google Patents、MCP 或 Playwright；只要能够取得真实对比文件，就允许用于发现和取文，但进入 CC 表前必须完成内容、日期、来源和可追溯性校验。
- 2026-07-19：正式业务代码之前仍先进行数据源验证，且官方、第三方、Google Patents、浏览器自动化和人工路径同场比较，不预设只有官方 API 才能入选。
- 2026-07-19：`PROJECT_CHARTER.md` 是整个仓库的强制首读文件，但其实质无效检索规则只约束无效系统及为其修改的共享边界；原侵权系统其他维护继续遵守根级和模块级 `AGENTS.md`。
- 2026-07-19：无效检索采用逐权利要求的多轮循环；每轮新文献先做单篇新颖性检查，未完整覆盖时选择/更新最接近现有技术、提取 gap，再进行定向检索和创造性组合分析。
- 2026-07-19：多文献合计覆盖全部特征不自动等于缺乏创造性；必须另行验证组合启示、技术问题、反向教导和技术效果。
- 2026-07-19：用户明确授权开始开发，并要求使用 `/Users/xiexiaoxiong/Documents/Patent项目配套/实例专利` 中的全部专利文件逐件测试；遇到障碍必须分析原因、修正并重跑，直到全部案例能够顺畅完成后才能停止。
- 2026-07-19：仓库只保留一份根级产品宪章；原侵权链路的单向调用规则不限制无效检索 `I0` 的持久化多轮循环，详细状态机写入 `docs/invalidity/WORKFLOW_SPEC.md`。
- 2026-07-19：第二/第三轮同时保留完整权利要求的单文献新颖性检索和区别特征/组合理由检索；新文献一律先做单篇比对，D1 可按轮次版本化替换。
- 2026-07-19：专利发现拆分普通现有技术与中国抵触申请候选两个日期通道；后续 gap 分为特征、证据、日期和组合理由四类。
- 2026-07-19：原侵权 `AGENTS.md` 与无效检索规则不并列充当两个主文件；仓库保持唯一宪章，无效工作流规范优先决定无效业务状态机，根级和模块级 `AGENTS.md` 分别决定工程及局部约束。
- 2026-07-19：D1 必须在真实取文、逐权利要求日期资格和单篇全限制比对后正式选择；D2/D3 按未解决 gap 的有效增量选择，不能把搜索排序或命中顺序直接当作法律分析角色。
- 2026-07-20：真实验证确认 Google Patents XHR/详情页及公开网页搜索存在限流和验证页风险；EPO OPS 被选为 MVP 首选官方全球专利主 provider 候选，完成 OAuth 与真实样本验收前不得宣称已具备全球检索能力。
- 2026-07-20：Yahoo 等公开搜索页只允许产生带查询/响应哈希的 `lead`，其检索批次固定为 `partial`；搜索摘要永远不能成为披露证据。
- 2026-07-22：根据用户最新决定，暂时停用 USPTO/EPO live 专利检索，改以智慧芽（Patsnap）作为主 provider 候选；等待用户提供 API 文档和凭据后进行适配与真实样本验收。EPO/USPTO 代码保留但不默认启用。
- 2026-07-23：用户确认当前及后续自动化专利候选发现统一优先使用智慧芽；当时采用的“主题 + 至少两个不同特征”约束已被 2026-07-26 的分角色、分字段检索规则取代，继续保留中英文平衡、禁止把传输重试或裸单特征宽搜冒充回退等审计要求。
- 2026-07-25：用户明确决定关键日按专利确定而非按权利要求核验：专利的申请日或最早优先权日一旦确定即适用于该专利全部权利要求，系统不得再逐项权利要求停车等待人工填写或核验日期；仅当专利级申请日和优先权日均缺失或无法解析时才转入人工复核。多优先权专利逐主题精细化留待后续由用户明确恢复。
- 2026-07-25：用户进一步明确当前测试和 MVP 自动分析只处理独立权利要求。模块一继续冻结全部权利要求事实，但从属权利要求不创建调查、不执行检索/分析、不参与状态或人工确认汇总，也不进入当前报告结论；律师模块实验室固定为“当前真实案件”和“内置示例”两种输入，底层 JSON 和技术模块仅在高级入口保留。
- 2026-07-26：用户明确调整首轮检索式逻辑：不再把所有权利要求限制都视为同层级“必要特征”，而是通读完整专利后区分保护客体/类别、类别共有技术语境和真正体现发明点的区别性特征；检索式必须同时决定词组最可能出现于标题摘要、权利要求或说明书全文中的哪一范围。首轮至少并行保留“分类/客体 + 发明点（通常全文）”和“客体同义词 + 类别结构语境（权利要求）”两条检索线，类别共有特征只用于召回和场景约束，不能直接支持新颖性评价。
- 2026-07-26：用户进一步放宽首轮检索式硬守门：技术特征关键词、保护客体/类别和分类号三类检索信息中任意两类实际出现即可执行，三类都有也可以；发明点精确线、语境召回线及其概念数量继续指导模型生成和字段选择，但不再额外淘汰已经满足“三类任二”的检索式。分类号允许作为有来源标记的模型检索建议参与检索，但不得冒充模块一事实或证据。第一次生成不合规时系统应自动携带失败原因重新生成，连续失败时允许确定性组装，不能因单次模型输出失败要求律师处理。
- 2026-07-26：用户提供 EPO OPS 测试授权并明确专利 provider 分工：智慧芽 P002 继续发现
  候选；系统根据智慧芽返回的公开号/申请号调用 EPO OPS 核验同一文献并下载其可取得的
  全文/PDF/页面。EPO 不作为检索式或零命中的候选搜索回退，取得内容后仍执行身份、哈希、
  日期和技术披露守门。
- 2026-07-27：用户确认智慧芽 P020 已开通，并调整专利取文顺序：P002 继续发现候选，I3-E
  先按同一公开号请求 EPO OPS；只有 EPO 正常收口并明确无覆盖或无可下载全文时，才以同一
  `patent_id + pn` 调用 P020 获取 PDF。EPO 鉴权、传输、限流、解析或身份错误不触发 fallback；
  P020 文件仍须通过真实下载、精确身份、PDF/MIME、字节冻结和哈希守门。
- 2026-07-27：用户明确模块四对每条专利检索式命中的前 10 篇候选全部取文，不再使用前 2
  或前 5 等隐藏抽样；成功取得全文的对比文件在模块五逐篇分析。模块五必须显示权利要求
  技术特征原文，并区分“明确未披露”与“证据不足暂不能确认”：理由明确认定文献未公开该
  特征时输出 `not_disclosed`，不得继续显示 `uncertain`。部分文献成功不能掩盖其他文献未
  分析或分析失败。
- 2026-07-29：用户确认模块三不得针对一份决定书或一组已知对比文件编写专用检索词，决定书
  只作为与运行时隔离的盲测答案；生产检索改为从任意目标专利提取客体、部件角色、连接拓扑、
  运动/状态、控制关系和技术作用，形成分字段短查询组合。
- 2026-07-29：用户确认模块四优先输出可读 JSON/Markdown，扫描 PDF 后台 OCR 并缓存，
  不阻塞其他文献；原始 PDF 已取得不等于全文可分析，模块五只能接收 `analysis_ready`
  文献。
- 2026-07-29：用户确认模块五采用整体结构关系标准：同词不是必要条件，同功能也不是充分
  条件；应结合部件角色、连接/运动关系和工作过程判断直接或必然隐含披露。
- 2026-07-29：用户进一步确认模块三采用多分辨率查询组合：保留一条可含多个发明点、以
  最大相似度为目标且允许零命中的长精确线，同时并行生成“客体+发明点”“客体分类+发明点”
  “客体+发明点分类”“客体+两个发明点”等短线；决定书仅在全部查询与结果冻结后检查
  对比文件 1 是否进入任一前十/待取文集合，不得将答案反向喂给运行时生成器。
- 2026-07-29：模块三短线进一步采用“目标原词 + 机构作用/动作等价词组”，客体词禁止缩成
  单一宽词；分类号角色未知时按目标专利已有分类号有界分线，不能只取第一个。最大相似长线
  保持完整锚点且不生成结构碎片。冻结回归中 D1 分别进入“客体+发明点”前十及
  `F41B9/00 + 客体 + 发明作用词`前十，满足验收门槛。
- 2026-07-30：用户将“每条检索前 10 篇全部直接取文”调整为“原始前 10 全部留痕、先做
  取文前候选清理、再对保留的唯一代表全部取文”。同一申请/同一公开号的 A2/A3/A4 等公开
  版本和可靠同族不得作为不同对比文件重复消耗全文接口；题录已经充分证明属于冰箱、光模块
  等明显无关技术领域且不含目标机构关系的候选可以排除。相关性判断必须保守：证据不足或
  可能存在跨领域机构启示时保留。模型只做批量技术语义分类，确定性规则负责身份分组、强
  正向锚点保护、失败时全量保留和审计，不作现有技术资格或无效法律结论。
- 2026-07-30：用户指出模块三已识别“位置选择性联接装置/中间位置选择性联接阀杆”后，却
  生成“解锁机构”检索词，属于发明锚点与可执行式脱节。检索式主锚点现固定优先使用目标
  原词或可回溯短语；机构等价词只能作同一动作机理内的 OR 扩展。联接不得自动推导锁定或
  解锁，历史缓存画像也必须按当前来源规则清理后才能重编译。
- 2026-07-30：用户进一步明确检索输入应由原子技术关键词构成，不得把
  “中间位置、选择性联接”这类两个概念的并列表述作为一个关键词。并列概念必须拆为不同
  关键词组；短线可只保留“选择性联接”，需要位置约束时再单独 AND“中间位置”。完整表达
  单一技术概念的术语（如“位置选择性联接装置”）仍可整体检索。
- 2026-07-31：用户进一步明确模块五不得按构件名称或权利要求措辞逐字比对。应从对比文件
  全文理解其整体机构与工作过程，允许名称、构件划分和外形不同但结构角色、介质/能量路径、
  连接、运动/状态及控制作用实质对应的方案认定披露；一个集成结构可以承接多个目标特征。
  对于全文机构必然要求但没有逐字记载的关系，可以用闭合的物理/机械必要性链认定必然隐含
  披露；但通用名词或相同功能本身仍不足以无条件等同，必须保存原文锚点并排除合理替代路径。
- 2026-08-02：用户明确测试阶段禁止 I2（模块三检索方案）复用既往结果：测试环境及律师
  模块实验室每次点击都必须重新调用模型完整生成发明画像与检索组合，任何情况下不得输出
  `same_source_cached_profile`；同源缓存画像复用仅保留给正式 release 和离线重编译脚本，
  测试服务对该来源 fail closed。
- 2026-08-02：用户明确首轮可执行检索词总数硬上限为 5 条，即 AI 判断最有希望的 5 条检索线。
  I2 首轮按确定性优先级保留：引证精确回查 > 说明书明确效果线（名词短语效果）> 客体分类+发明点
  > NPL 完整权利要求线 > 双发明点 > 标题摘要 > 结构召回 > 单发明点短精确 > 最大相似长线；
  纯动作族辅助效果线（控制/定时/联接等无名词效果词）降级排后，抵触申请日期通道在上限内折叠；
  被裁减的检索线进入 `trimmed_sink` 并写入 `portfolio_warnings`，与「未形成」线区分，全程可审计。
  单发明点画像不再生成与单发明点线重复的双发明点线。同时再次确认并核查：模块三任何规则不得
  刻意针对某一篇专利锁定发明点或编写案件特定分支；所有词族/等价词规则必须由目标专利自身文本
  驱动、对任意专利通用，历史「水枪|喷枪」单案件效果正则已泛化为通用效果短语抽取。
- 2026-08-08：用户明确创造性检索采用新的区别技术特征循环：首轮完成后以已确认相同技术
  特征数量最多的日期合格文献作为 D1（并列再比较领域、作用、改动和证据完整度），把 D1
  未公开特征及其内容/结构角色/技术作用冻结为区别特征；新增 `I2_GAP_QUERY_PLAN`，先检查
  当前已完成 I4-S 的现有文献是否已经公开全部区别特征且作用相同，已覆盖则不再检索，未覆盖
  才只围绕剩余区别特征生成查询。每次循环执行检索、取文/复用、I4-S 和覆盖复核，覆盖成功
  立即停止，失败则基于仍未覆盖项和失败原因改写下一轮，首轮之后最多 5 次；第 5 次仍失败也
  进入组合分析。最终选择 D1 及按相同特征数/区别特征增量排序的前 5 篇合格文献输出大 CC 表。
  复用只允许命中文献版本、内容哈希、目标特征版本、日期资格和 I4-S 规则版本完全一致的持久化
  结果。系统目标是尽量形成覆盖全部独立权利要求特征的可核验证据链，不承诺或直接作出最终无效结论。
- 2026-08-08：用户确认律师模块实验室由七个顶层业务模块调整为十个可分别测试的业务阶段：读取目标专利、确定关键日、总结核心发明点、生成首轮检索词、首轮检索与证据核验、单篇新颖性比对、选择 D1 与冻结区别特征、区别特征检索循环、创造性组合分析、大 Claim Chart 与律师报告。底层 I0—I5 模块边界不变；本次只把原模块六中折叠的 I4-S/I4-C/I4-I 和 D1 后 I2-G 循环按律师工作顺序拆开展示。每个阶段保留当前真实案件/内置示例两个测试入口和独立运行状态；第八阶段按 gap 轮次展开，不把每一轮提升为新的固定顶层模块。
- 2026-08-08：用户进一步确认在 D1/区别特征冻结与进一步检索之间新增 `I4_O_OBVIOUSNESS_PRECHECK`「显而易见性预分析」，律师实验室扩展为十一个阶段。区别特征不能因 D1 未直接公开就自动进入结构 gap 检索；系统须先分析客观技术问题、D1 内部启示、惯用技术手段/公知常识、改造动机、反向教导和效果可预期性。D1 具体启示与惯用技术手段是可分别成立的两条路径；任一路径成立且改造动机/路径明确、无反向教导、效果可预期时跳过普通结构检索。惯用手段只属模型预判时仅补公知常识证据并标律师确认，不能冒充证据完整。`I2-G` 改为只消费预分析未解决的直接特征、公知常识或组合证据目标，`I4-I` 支持 D1+D2/D3、D1+公知常识和 D1 内部启示+惯用替换三类路径。
- 2026-08-04：用户明确将律师模块实验室原业务模块三「检索方案」拆分为两个相互独立的业务模块。新模块三「总结核心发明点」调用新底层模块 `I2_INVENTIVE_PROFILE`，只输出 AI 总结的核心发明点一段话（`invention_summary`）、发明点区别特征及其对应技术特征，不生成检索式；新模块四「生成检索关键词」沿用 `I2_QUERY_PLAN` 原行为，内部独立重新生成画像并编译检索关键词/查询组合，不读取模块三的运行结果。两个模块互不依赖、可分别单独运行；业务模块总数由六个变为七个，原四至六模块顺延为五至七；I0–I5 底层模块边界与正式 I0 流水线不变。
- 2026-08-04：用户进一步明确律师模块实验室模块三/模块四的输入关系（取代同日早些时候「两个模块相互独立」的表述）：新模块四「生成检索关键词」的输入固定为同案同独立权利要求最新成功的模块三「总结核心发明点」输出（`invention_summary` 核心发明点总结、发明点区别特征及其绑定的技术特征，即发明画像 + limitations），不再向律师展示或重新读取专利摘要、说明书、独立权利要求原文，也不再重新调用 GLM 生成画像；模块四据此以免模型确定性重编译方式生成检索关键词/查询组合（显式 `same_source_cached_profile` 重编译路径，输出携带 `source_plan_run_id` 溯源）。没有对应成功模块三运行时，模块四必须明确失败（`LAB_I2_PROFILE_REQUIRED`）并提示先运行模块三，禁止静默回退重新生成画像。2026-08-02 的禁复用守门（`LAB_I2_CACHED_PROFILE_FORBIDDEN`）相应收窄为只拦截 `plan_queries` 引擎内部缓存复用，不拦截这条律师显式的「模块三输出 → 模块四确定性重编译」路径。该决定只约束律师模块实验室的模块三/模块四；正式 I0 流水线的 I2 仍按原行为自行生成画像并编译检索式，不受影响。
- 2026-08-05：用户进一步明确模块四生成检索词必须重新调用大模型（修订 2026-08-04 第二条决定中的「免模型确定性重编译」）：模块四的输入仍是同案同独立权利要求最新成功的模块三输出（发明画像 + 技术特征），不重读专利全文、附图或权利要求原文；但模块四每次运行都必须以该画像为输入重新调用 GLM，由模型按多分辨率检索线角色分析并提出检索关键词/查询组合。模型提案继续经过全部既有确定性守门（三类任二、中文关键词 ≤6 字原子化、首轮 5 条上限优先级裁减），第二次模型响应仍不合规时才允许用画像已冻结事实确定性组装兜底并保留完整审计。输出来源标记为 `module3_profile_live_model`，`source_plan_run_id` 溯源保留；无对应成功模块三运行时仍明确失败 `LAB_I2_PROFILE_REQUIRED`。模块四该次 GLM 调用是纯文本子任务（不需要理解附图或扫描页），经显式 `allow_text_only` 放行且模型仍为 GLM-4.6V，不构成 4.7 禁止的静默纯文本降级；需要图文理解的其他任务仍按原规则 fail closed。正式 release 与离线脚本继续保留 `recompile_initial_query_plan` 免模型重编译路径；`LAB_I2_CACHED_PROFILE_FORBIDDEN` 守门与正式 I0 流水线 I2 行为不变。
- 2026-08-05：用户针对真实运行 `c32ffcef`（无线耳机案模块四输出）明确两条检索词构建规则：①每条可执行检索式的每个关键词都必须用 OR 并联同义词及中英文对照（英文锚点配中文同义词、中文锚点配英文对应词），同义词只能来自目标专利发明画像/权利要求中已冻结的事实（客体同义词、特征绑定的中英文同义词与机构等价词、权利要求原文词的同族词），画像确实没有同义词时允许单原词放行，不得编造画像中没有的词；②论文/技术资料（NPL）检索式也必须是关键词 AND/OR 组合，不得使用整句话检索——废除 2026-08-03 原子粒度规则中「无显式 AND/OR 的 NPL 自然语言整句线保持原样」的豁免，NPL 线与其他检索线一样经过客体核心名词化和 ≤6 字原子化。同时，规范化（大小写/空白/标点）后完全相同且同 provider、同检索范围、同日期通道、同分类号的重复检索式必须去重，被去掉的写入 `portfolio_warnings` 留痕；分类号分线与抵触申请日期通道折叠等刻意设计不受影响。上述规则对任意专利通用，适用于全部 I2 检索线、确定性组装、离线重编译与模块四 GLM 生成路径。
- 2026-08-03：用户明确可执行检索关键词的原子粒度规则（任务 12328641 耳机案件暴露问题）：
  单个可执行关键词中文不超过 6 个字，超过必须提炼为原子技术名词。中文客体必须使用核心类别
  名词（剥离「适配于不同耳朵的」「无线」等修饰定语），配同义 OR 组（如 耳机/耳麦）；修饰语
  确需保留时只能拆成独立 AND 关键词（如 无线 AND 耳机），不得留在客体词内部。权利要求整句
  （所述……具有/设置有/电性连接……）禁止作为检索词，必须提炼为原子技术名词后 AND 组合
  （如 无线 AND 耳机 AND PCBA板 AND 喇叭 AND 电池 AND 出音孔）。该上限适用于中文原子词；
  英文客体/特征仍保留完整短语规则（water gun 不得裁成 water）。规则对任意专利通用，适用于
  I2 全部检索线、确定性组装与离线重编译。
- 2026-08-06：用户明确 I4-S「实质相同」披露标准的最终解释，并批准针对 v45 重跑
  （run `21545a18`，水枪专利 × US20200080816A1）仍暴露的两类机制性缺陷增加确定性守门：
  ①**实质相同标准**：比对必须基于对比文件全文理解其整体机构后判断；特征名称不同不构成否定。
  「实质相同」指结构和位置关系与目标专利没有不同、且功能作用相同；对比文件中的下位概念或
  具体构件只要承担了目标特征的相同角色（如钉子承担固定单元的作用），即视为公开，不要求
  名称、构件划分或具体结构完全相同。该解释是对既有整体机构对应标准（7.7 第 6 条）的澄清，
  不放宽必然隐含披露的物理/机械必要性链要求。
  ②**否定断言核验守门**：凡特征结论为 `not_disclosed`（或理由为「未提及/未发现/无对应」的
  否定断言），确定性层必须围绕「承担该特征角色的候选构件/结构区域」做核验——特征核心词、
  绑定同义词及候选角色词在对比文件全文中存在而未在推理或结构证据中被逐一处理的，不得直接
  输出 `not_disclosed`；应触发一次有界候选复核（模型逐条说明每个候选为何不对应），复核后
  仍有未处理候选的降级为 `uncertain` 并留痕。
  ③**跨特征一致性对账守门**：全部批次与结构复核完成后，确定性层对特征树做一致性检查——
  父特征已经角色映射判披露的，其子特征不得以「无该结构/未提及」为由判 `not_disclosed`；
  `reference_mechanism_summary` 或其他特征证据/映射中出现的构件，不得在另一特征层被判
  「全文未提及」。检出矛盾时把矛盾双方连同双方推理放进同一上下文做一次有界合并重判；
  重判后仍矛盾的降级为 `uncertain` 并写入告警。模型负责语义理解，上述触发、对账、降级与
  留痕由确定性规则执行。规则对任意专利通用，适用于全部 I4-S 路径。
- 2026-08-06（同日第二条）：用户明确 I4-S 在逐特征判断之前增加「候选构件发现」预检步骤。
  模型先基于对比文件全文，为每个目标特征的角色列出对比文件中可能承担该角色的候选构件/
  结构区域清单（含原文引文与位置、承担该角色的理由，允许显式「无候选」），再带着这份
  共享候选清单做逐特征功能比对——比对的对象从「全文里现场找」变为「对候选清单逐一做
  功能核对」。候选清单注入首轮各批次、整体结构复核与守门复核的 prompt，全部批次看到同一
  份清单，从源头降低跨特征矛盾；清单中每条引文必须经确定性层回验真实存在于对比文件
  全文，无法回验的条目丢弃并留痕，防止模型编造候选；发现步骤调用失败不阻断既有比对
  流程，降级为无清单运行并记录审计字段；候选清单与回验结果持久化到 run 输出，供律师
  查看和后续模块溯源。该步骤是对实质相同标准（同日第一条）执行方式的强化，不改变
  披露判断标准本身与两道守门。
- 2026-08-09：用户根据固定五组真实检索全部零命中的结果，明确 I2 仍保持五条固定线和字段
  结构，但每个过细客体/特征/功能词组必须在同一 OR 组中加入可回溯的核心含义词或合理上位
  词；例如具体喷水玩具车并列“喷水车/玩具车/玩具水车”，具体供水管路并列“供水管/供水”，
  具体水中吸水结构并列“吸水结构/吸水”。上位词用于扩大召回，不得替换或删除具体词，不得
  另建裸宽词检索线，也不得从已知对比文件或决定书反推；OR 组成员上限与跨语言平衡继续适用。
- 2026-08-09：用户确认模块六允许更长运行时间，并要求避免十分钟后显示“无可用输出”的假失败，
  同时评估并采用受控并行加速。由此冻结：模块六改为持久化、可恢复、逐篇保留结果的 I4-S 批次；
  排队与执行分别计时，前端固定超时不得改写后端任务状态；测试服务默认启动 2 个 durable worker，
  可配置但最多 4 个，依靠数据库租约竞争任务并在停机时分别释放；该批次只负责 I4-S，不越级自动
  创建 I4-C 或 I4-I。
- 2026-08-09：同一案件的旧模块六运行暴露“前端停止后仅部分文献创建 I4-S”的历史恢复缺口。
  用户要求不得再出现长耗时后丢结果，因此确认无 batch ID 的旧运行必须以最新 I3 候选筛选血缘下的
  全部可分析全文重建批次清单：复用已有 I4-S，自动补建遗漏文献，禁止用固定时间窗猜测清单。
- 2026-08-09：用户进一步确认模块九必须是真正的逐区别特征自动多轮闭环，而不是生成一轮
  查询并在 I4-S 后结束。每个 gap 轮必须为每项仍未由可引用证据关闭的区别技术特征至少生成并
  执行一条绑定 feature ID 的主检索线；I4-S 后逐特征关闭已有披露依据的项目，其余自动进入下一轮。
  下一轮必须依据上一轮未命中/未覆盖原因实质换词，可扩展到同一上位类别下的相邻产品类别、该
  特征通常出现的具体产品/部件类别，或其角色、结构、动作、路径和效果表达；最多自动运行 5 个
  gap 轮，逐轮保存查询与覆盖谱系。
- 2026-08-10：用户进一步冻结模块九逐特征查询的固定结构：每轮普通专利主检索组合数必须等于
  当前未解决区别特征数，一项特征恰好一组；每组只由“非目标专利完全相同类别的邻近/上下位
  客体中英文 OR 组”AND“该区别特征的中英文及上下位/同义特征点 OR 组”构成。未取得合格
  披露时下一轮保持相同组数并实质换词，最多五轮；附加证据通道不计为新的区别特征组。
- 2026-08-11：用户确认模块九的律师界面改为逐轮显式推进：模块九仍是单一顶层业务阶段，
  但一次点击最多收口一个 gap 轮，当轮独立展示后由用户点击“继续下一轮”。I4-S 失败文献
  允许在同轮只重试失败项，不重跑成功文献和 provider 检索；当轮与累计统计必须分开。
- 2026-08-12：用户指出模块四仍把“具有升降结构”作为完整检索词。由此冻结通用撰写外壳
  清洗规则：发明画像保留完整原文，查询编译剥离“具有/具备/带有/设有/设置有/采用”等前缀
  及“结构/装置/机构/系统/组件/部件/单元”等无区分度载体后缀，提炼核心动作/功能/部件词；
  “具有升降结构”应成为绑定原特征的“升降”，不得进入客体同义词组；带该属性的完整标题
  应把产品类别“室外消火栓”作为客体。规则适用于全部案件和 I2 编译路径，不使用案件词表。
- 2026-08-15：用户确认模块九在首次进入时一次生成并冻结全部 5 轮检索矩阵，后续仍逐轮
  显式执行；区别特征解决后跳过其后续行。跨轮重复命中文献时保留新命中血缘，仅在完整版本
  绑定一致时复用 I4-S，D1 或组合语料变化时仍重新运行 I4-O/I4-I。
- 2026-08-15：用户确认模块十必须承接模块九最多五轮新增文献的当前版本逐篇比对，并按中国
  创造性判断三步法形成逐区别特征证据闭环：D1 固定区别特征与技术问题，D2/D3、现有技术或
  公知常识逐项补齐披露，再核验结合启示/修改动机与路径、反向教导和效果可预期性。累计覆盖
  不能单独推出缺乏创造性；证据链不闭合时只能写“现有证据尚不足以证明不具备创造性”，不能
  反向写成专利确定具有创造性或有效。
- 2026-08-16：用户确认模块十一固定包含三部分：模块十创造性三步法结论的律师可读文字版；
  相似度最高的 10 篇对比文件横向特征矩阵；全部已检索并完成逐篇分析的对比文件完整 CC。
  前十不足时如实展示；排序以当前 I4-S 已确认披露数量/覆盖比例和证据完整度为确定性依据，
  待确认不算披露，禁止用文本向量或历史批次替代当前特征事实。
- 2026-08-16：用户进一步收敛测试产品入口：Agent 侧边栏只显示两个大型测试页——现有 11 模块
  高级无效检测实验室，以及汇总现有专利分析模块的专利分析实验室；不再向普通用户展示其他
  无效测试页面或零散测试入口。内部细分路由可为兼容和排错保留，但不构成新的用户侧测试产品。
- 2026-08-16：用户进一步明确专利分析实验室的页面结构：顶部统一输入一次当前测试专利，下面
  依次按专利解析、关键词生成、商品检索、权利要求与商品比对拆成可分别测试的模块；每个模块
  单独显示本次实际输入和输出，模块及长内容默认收起，后序模块只承接同一测试会话的前序结果。
- 2026-08-16：用户把正式 Agent 的后台分析路由改为显式选择：输入区附件按钮旁新增“专利无效”
  与“专利侵权分析”两个可独立切换的按钮。只有选中的类型才允许创建对应后台任务；两项都选时
  分别启动两条隔离工作流，两项都不选时即使输入文字提到分析或带有附件，也只由大模型直接回答，
  不得由意图模型、附件类型或关键词自行追加后台工作流。
- 2026-08-16：用户明确当前部署按正式环境使用，并授权把测试环境现有的智慧芽与 EPO OPS 外部
  provider 凭据值显式配置到正式环境。该授权只允许一次性写入正式前缀变量和正式权限文件，不得
  引入运行时测试变量回落；正式 5109、`invalidity_prod`、API token、上传根、工件目录、PM2 状态和
  不可变 release 继续隔离。Agent 只有在用户提交文件/需求并显式选择“专利无效”后，才启动正式
  I1—I5 无效工作流；本次部署本身不得预先创建案件。
- 2026-08-16：用户纠正此前“正式/测试后端隔离”的理解：正式 Agent 与测试页面只是同一业务的
  两种前端视图，不得各自连接一套后端。专利无效以后续一直调试的 11 模块无效链为唯一后端，
  专利分析以后续一直调试的四模块链为唯一后端；Agent 负责自动完整编排，测试页负责逐模块查看
  输入输出。历史 5109/`invalidity_prod` 和其他双栈配置退出用户请求链路，保留只读迁移审计即可。
- 2026-08-16：用户要求正式 Agent 需要用户介入时直接显示确认。gap 下一轮仅属调度确认，显示 30 秒
  倒计时，用户可立即开始，超时且状态版本未变则幂等自动开始；关键日、证据资格等法律事实确认不得
  超时自动决定。真实运行同时暴露 I2-G 把“消防设施”等上位类词误判为“室外消火栓”完全相同类别，
  因此完全相同类别守门改为只拦实际等同客体，混合词组过滤精确客体后仍满足双语即可继续。
- 2026-08-16：用户进一步明确同一无效分析只有一个后台任务、两种前端视图：Agent 在对话内向普通
  用户直接展示简明进度、结果和确认，不再把普通用户送入技术型 `/invalidity/results`；
  `/test/module-lab` 仅供开发管理员按同一 session/investigation 查看十一阶段详细输入、输出、轮次和
  报错。诊断视图不得为查看同一任务复制调查或另起工作流，入口与访问均受管理员权限控制。
- 2026-08-19：用户明确模块九第 2 个 gap 轮不能只选上位客体；目标客体已过宽或继续上位无检索
  价值时，应选择与区别特征相符的具体下位产品并说明理由。五轮中任一轮或多轮失败都只形成可审计
  的 partial/error，不得阻断其余冻结轮次；除特征已解决或用户取消外，必须把五轮全部执行/跳过
  收口。模块十不得因模块九全部失败或没有新增对比文件而停止，须基于首轮及当时既有合格语料降级
  输出三步法分析，并逐轮列明模块九错误和证据局限。
- 2026-08-23：用户要求把无效 Agent 已采用的“工作动态/进度过程卡 + 独立最终结果回复”模式复制到
  专利侵权分析；过程与结果不得混在同一卡片。专利分析测试产品只保留一个四阶段实验室，页面不使用
  “模块1结果”等技术编号标题；专利解析、关键词生成、商品检索、权利要求与商品比对每一阶段均须
  分开展示本次输入、本次输出和结果，并沿用无效实验室的卡片与状态设计。
- 2026-08-23：用户要求管理员可从侵权 Agent 当前过程卡直接打开该次分析的四阶段诊断。入口必须绑定
  当前 `analysis_session_id`，页面只读恢复同一任务的真实输入、输出、结果、状态和错误；查看/刷新
  不得创建测试运行、重跑模块、改写旧审计或串用其他 session 数据。

## 10. 版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| 2.42 | 2026-08-23 | 侵权 Agent 管理员过程卡新增当前 `analysis_session_id` 的四阶段诊断入口；`/test/product-pipeline?session=...` 作为管理员同任务只读视图，恢复该 session 的真实输入、输出、结果、状态和错误，查看/刷新不得重跑、复制、改写或跨 session 串用结果。 |
| 2.41 | 2026-08-23 | 侵权 Agent 与无效 Agent 统一为“启动回复 -> 可核验工作动态/阶段进度过程卡 -> 独立 assistant 结果回复”；专利分析测试产品收敛为唯一四阶段实验室，阶段标题使用业务名称而非“模块1结果”，每阶段分开展示本次输入、本次输出和结果，并沿用无效实验室的卡片层级。 |
| 2.40 | 2026-08-19 | 模块九第 2 轮改为优先上位、无法形成有价值上位客体时选择合适下位产品；任一 gap 轮错误不得阻断其余冻结轮次，五轮必须执行/跳过收口；模块十即使模块九全部错误或无新增文献，也必须使用首轮和既有合格语料降级输出并列明模块九失败谱系。 |
| 2.39 | 2026-08-16 | 同一无效任务固定为一份后台事实、两种视图：正式 Agent 在对话内直接给普通用户展示简明进度、结果与必要确认，不再导航到技术型 `/invalidity/results`；开发管理员可在 `/test/module-lab` 按同一 session/investigation 查看十一阶段详细输入输出和错误，诊断不得复制或另起任务，入口与访问均限管理员。 |
| 2.38 | 2026-08-16 | 正式 Agent 的 gap 续轮检查点在工具卡片内显示 30 秒确认，显式确认立即执行、超时且状态版本一致时幂等自动执行；法律事实/证据资格确认仍必须由用户提交。I2-G 完全相同客体守门只拦实际等同客体，混合客体组过滤精确词后仍满足双语即可继续，避免把上位类别误杀为整组失败。 |
| 2.37 | 2026-08-16 | 用户明确每项业务只保留一套后端：无效 Agent 与 11 模块实验室共用现有无效测试模块链；专利分析 Agent 与四模块实验室共用现有专利分析测试模块链。正式/测试只表示前端使用方式，不再对应不同服务、schema、token、上传根、工件或规则版本；历史双栈退出用户请求路径。 |
| 2.36 | 2026-08-16 | 用户明确当前部署按正式环境使用，并授权将测试环境现有智慧芽/EPO OPS 外部凭据值显式写入正式隔离配置；运行时仍禁止跨环境回落，正式 5109/schema/token/upload/release 保持独立。Agent 仅在收到文件/需求且用户显式选择“专利无效”后启动既有 I1—I5 正式工作流，本次部署不预建案件。 |
| 2.35 | 2026-08-16 | Agent 输入区新增“专利无效”和“专利侵权分析”两个独立显式开关；按钮状态成为后台工作流的唯一启动依据，两项都不选时只进行大模型问答，禁止文字意图或附件自行触发分析。 |
| 2.34 | 2026-08-16 | 专利分析实验室改为顶部统一专利输入 + 四个可分别运行的业务模块；每个模块分别展示本次实际输入、输出、状态和失败原因，长内容默认收起，并禁止后序模块跨测试会话静默复用历史结果。 |
| 2.33 | 2026-08-16 | 测试产品入口收敛为两个大型页面：现有 11 模块高级无效检测实验室和专利分析模块实验室；两者并列置于 Agent 侧边栏，其他细分无效/测试路由只允许作为内部兼容或调试深链，不再作为普通用户入口。 |
| 2.32 | 2026-08-16 | 正式 Portal 根入口改为单一对话式专利 Agent：同一输入框接收专利问题、文本、URL 和文件附件，由受控模型路由为普通问答、侵权分析、无效分析、明确要求下的双流程或补充询问；现有两套后台、状态机和结果表保持不变，Agent 只承担鉴权后的意图识别、工具编排、进度恢复和结果解释。`/test` 页面保持原样。 |
| 2.31 | 2026-08-16 | 模块11报告改为三部分：当前模块10三步法结论的律师可读文字分析；按当前 I4-S 已确认披露数/覆盖比例及证据完整度选出的最多10篇对比文件横向特征矩阵；全部已逐篇分析对比文件的完整 CC。三部分均绑定当前单次业务运行，前十不足如实展示，不用文本相似度或历史批次补足。 |
| 2.30 | 2026-08-15 | 模块10按创造性判断三步法消费模块7的当前D1、模块8预分析以及模块9最多五轮全部日期合格/current-I4-S补证语料；逐区别特征输出披露、相同作用、结合启示/动机与路径、反向教导和效果可预期性的闭环矩阵。只有累计覆盖与结合路径均完整时才称“已形成缺乏创造性的完整证据链”；否则仅称“尚不足以证明不具备创造性”。 |
| 2.29 | 2026-08-15 | 模块9首次进入时一次生成并冻结第1—5轮完整检索矩阵，后续仍一次执行一轮；已解决特征的未来行跳过。跨轮重复文献保留新命中血缘，仅在完整版本绑定一致时复用 I4-S，D1/语料变化仍重跑 I4-O/I4-I；前端一次展示五轮计划并默认折叠。 |
| 2.28 | 2026-08-15 | 模块9的 5 个 gap 轮改为固定语义递进阶梯：相邻客体+直接结构、上位客体+结构族、同功能客体+动作/角色、子系统客体+关系/路径、类比领域+原理/效果；每轮仍一特征一组双语主查询，确定性编译器不得以同词池换序冒充新一轮。 |
| 2.27 | 2026-08-12 | I2 可执行词新增通用专利撰写外壳清洗：画像保留原文，编译时把“具有升降结构”等属性短语提炼为绑定原特征的核心词“升降”，产品类别单独作为客体；属性短语不得冒充客体同义词，全部 I2 路径统一守门。 |
| 2.26 | 2026-08-11 | 模块九保持单一顶层有界循环，但律师界面每次只推进一个持久化 gap 子批次；当轮收口后显式继续下一轮，I4-S 失败只重试失败文献，不重跑已成功文献或检索；当轮与累计统计分开，禁止跨轮分母错位。 |
| 2.25 | 2026-08-10 | 模块九主检索组合固定为“一项未解决区别特征一组”：每组采用非目标完全相同类别的邻近/上下位客体中英文 OR 组 AND 该特征的中英文及上下位/同义特征点 OR 组；普通专利主组合数等于未解决特征数，未命中时逐轮保持组数并实质换词，最多五轮，附加证据通道不冒充区别特征组。 |
| 2.24 | 2026-08-09 | 模块九冻结为逐区别特征的自动多轮闭环：每轮为每项尚无可引用证据关闭的区别特征至少执行一条绑定 feature ID 的主检索线，I4-S 后逐特征关闭或自动续轮；下一轮须依据未命中/未覆盖原因实质换词，并可扩展相邻产品类别、特征具体产品/部件类别及角色/结构/动作/路径/效果表达，最多 5 个 gap 轮。 |
| 2.23 | 2026-08-09 | 历史无 batch ID 的模块六恢复改以最新 I3_FETCH 的 candidate_filter_run_id 血缘和 analysis_ready 全文为权威清单；已有 I4-S 逐篇复用，未创建的文献补建，禁止按时间间隔猜批次或只恢复已有比较而漏文献。 |
| 2.22 | 2026-08-09 | 模块六 I4-S 多文献比对改为持久化可恢复批次：逐篇保存排队、执行、终态和输出，刷新/断网/客户端等待到期不得丢失已完成结果；排队时间与执行时间分离展示；测试服务通过独立 worker 身份和数据库租约启用默认 2、上限 4 的受控并行；模块六批次只运行 I4-S，不自动越级创建 I4-C/I4-I。 |
| 2.21 | 2026-08-09 | 固定五组改为“具体词 + 核心含义/合理上位词”同组 OR：保留具体客体/特征/功能词，同时加入由目标专利冻结事实或模型通用化分析支持的核心产品类别、部件/动作词，避免过细 AND 组合导致零召回；上位词不得替换具体词、另建裸宽线或从盲测答案反推，每组仍受 OR≤5、中文原子化和跨语言平衡约束。 |
| 2.20 | 2026-08-09 | 固定五组首轮检索改为“生成阶段校验一次、执行阶段按冻结组合原样发射”：同案成功 I2 的固定变体以 plan run、query_id 和 provider_expression 精确回读后，不再被旧通用单条守门以“只有技术主题”或“原始长特征未逐字出现”拒绝；任意临时、跨案或表达式不一致输入继续 fail closed。 |
| 2.18 | 2026-08-08 | 在 I4-C 与 I2-G 之间新增 `I4-O` 显而易见性预分析；先分析 D1 内部启示、惯用手段、公知常识、改造动机、反向教导和效果可预期性，再把未解决项分流为直接特征、公知常识、组合证据或人工复核。律师模块实验室扩展为 11 阶段，I4-I 支持 D1+公知常识及 D1 内部启示+惯用替换路径。 |
| 2.19 | 2026-08-08 | 明确 I4-O 的 D1 具体启示与惯用技术手段/公知常识是两条可分别成立的显而易见性路径；任一路径成立且动机/修改路径明确、无反向教导、效果可预期时都不得重复普通结构检索，惯用手段仅待补证时只生成公知常识证据查询。 |
| 2.17 | 2026-08-08 | 按用户决定把律师模块实验室从七个折叠业务模块调整为十个可分别测试的业务阶段：读取目标专利、确定关键日、总结核心发明点、生成首轮检索词、首轮检索与证据核验、单篇新颖性比对、选择 D1 与冻结区别特征、区别特征检索循环、创造性组合分析、大 Claim Chart 与律师报告；每阶段保留当前真实案件/内置示例入口和独立状态，第八阶段按轮次展开，底层 I0—I5 边界不变。 |
| 2.16 | 2026-08-08 | 按用户决定新增创造性区别技术特征检索循环：D1 先按已确认相同特征数最多选择；冻结区别特征的内容、整体结构角色和技术作用；新增 `I2_GAP_QUERY_PLAN` 先复核当前合格 I4-S 文献能否以相同作用覆盖 gap，覆盖则不检索、未覆盖才只针对剩余 gap 生成新词；每轮执行检索→清理→取文/复用→I4-S→覆盖复核，成功立即停止，失败按未覆盖项和失败原因重编译，首轮后硬上限 5 次；第 5 次后进入组合分析和大 CC 表；大 CC 默认展示 D1 及相同特征/区别特征增量最高的前 5 篇合格文献；缓存复用须绑定文献版本、内容哈希、目标特征版本、日期资格和 I4-S 规则版本。 |
| 2.15 | 2026-08-06 | 按用户决定：首轮检索固定为五组线并取代多分辨率首轮组合——①客体+同专利权人其他专利（申请人字段，仅一组，排除目标专利本身，申请人缺失跳过留痕；2026-08-07 真实 P002 验证确认 AN 有效而 NOT PN 返回 68300004，故供应商执行 AN+客体并冻结原始排序，本地按规范化公开号排除目标）；②标题检客体及中英文同义词 AND 说明书检核心发明点及同义词；③客体相关分类号 AND 说明书检核心发明点及同义词；④说明书检客体及同义词 AND 说明书检核心发明点及同义词 AND 说明书检技术效果词；⑤标题和关键词检客体及同义词 AND 说明书检核心发明点功能描述及同义词（承载 2.14 自创术语功能表达）；同义词来源、最终 OR≤5、≤6 字原子化等守门照旧，首轮不超过五条、事实不足的组跳过留痕、零命中不创建 compact fallback；规则对任意专利通用。本次按 2.13 约定同步重跑五案 I2 计划冻结。 |
| 2.14 | 2026-08-06 | 按用户决定：发明点自创术语改写——自创/低通用度命名（如「位置选择性联接装置」）不得单独作 AND 锚点承载首轮召回线；画像须为每个发明点特征额外产出功能表达（上位通用构件词 + 功能效果词组及中英文同义，来源标记与既有原子化/OR 守门照旧）；首轮检索线混编——一部分线采用功能表达（客体+通用构件于权利要求层 AND、功能效果词组于说明书/全文层 AND），另一部分线保留原术语表达，自创词术语线不得独占首轮位置；规则对任意专利通用。本次按 2.13 约定同步重跑五案 I2 检索计划冻结。 |
| 2.13 | 2026-08-06 | 按用户决定：内置盲测必须跟随规则升版——模块六（I4-S）判断规则每次升版后，须用当前规则对五个真实无效内置盲测案件全部比对单元重新逐篇分析并原子冻结为新版本目录，同时把模块实验室前端白名单指向新冻结版本；内置盲测展示必须始终对应当前规则版本，禁止长期展示旧规则冻结结果；I2 检索计划冻结同理在检索规则升版时重跑。本次随即按 structural-disclosure-v50 执行重冻结（`20260806-five-case-v50`）。 |
| 2.12 | 2026-08-06 | 按用户决定：I4-S 在逐特征判断之前增加「候选构件发现」预检步骤——模型先基于对比文件全文，为每个目标特征的角色列出对比文件中可能承担该角色的候选构件/结构区域清单（含原文引文与位置、角色理由，允许显式「无候选」），再带着这份共享候选清单做逐特征功能比对；候选清单注入首轮各批次、结构复核与守门复核 prompt，全部批次看到同一份清单以降低跨特征矛盾；清单中的引文必须经确定性层回验真实存在于对比文件全文，无法回验的丢弃并留痕；发现步骤失败不阻断既有比对流程，降级为无清单运行并记录审计；候选清单与回验结果持久化到 run 输出。 |
| 2.11 | 2026-08-06 | 按用户决定（v45 重跑 run `21545a18` 仍暴露缺陷）：澄清 I4-S 实质相同标准——基于对比文件全文理解整体机构，结构和位置关系与目标无不同且功能作用相同即为实质相同，下位概念/具体构件承担相同角色（钉子之于固定单元）即视为公开，不要求名称或结构完全相同；新增两道确定性守门：否定断言核验（`not_disclosed`/「未发现」结论须逐一处理全文中的候选构件，未处理则触发有界候选复核、复核后仍遗漏降级 `uncertain`）与跨特征一致性对账（父披露子不得「无该结构」否定、机构总结出现的构件不得被判「全文未提及」，矛盾双方合并重判一次、仍矛盾降级留痕）；触发、对账、降级与留痕由确定性规则执行，规则对任意专利通用。 |
| 2.10 | 2026-08-05 | 按用户决定（真实运行 `c32ffcef` 暴露问题）：每条可执行检索式的每个关键词必须 OR 并联同义词及中英文对照，同义词只能来自画像/权利要求已冻结事实，画像无同义词时单原词放行、不得硬造；NPL 检索式必须是关键词 AND/OR 组合、不得整句检索，废除 2026-08-03「NPL 自然语言整句线保持原样」豁免，NPL 线同样经过客体核心名词化与 ≤6 字原子化；规范化后完全相同且同 provider/范围/通道/分类号的重复检索式去重并写 `portfolio_warnings` 留痕；规则对任意专利通用，适用于全部 I2 检索线、确定性组装、离线重编译与模块四 GLM 生成路径。 |
| 2.9 | 2026-08-05 | 按用户决定（修订 2.8 中「免模型确定性重编译」）：律师模块实验室模块四的输入仍为模块三最新成功输出（发明画像 + 技术特征，不重读专利），但每次运行必须以该画像为输入重新调用 GLM 分析生成检索关键词/查询组合；模型提案继续经过全部既有确定性守门（三类任二、≤6 字原子化、5 条上限裁减），连续不合规才确定性组装兜底；来源标记 `module3_profile_live_model`；该纯文本子任务经显式 `allow_text_only` 放行、模型仍为 GLM-4.6V，不构成静默纯文本降级；`recompile_initial_query_plan` 保留给正式 release/离线脚本，禁复用守门与正式 I0 流水线不变。 |
| 2.8 | 2026-08-04 | 按用户决定（取代 2.7 中「两个模块相互独立」的表述）：律师模块实验室新模块四「生成检索关键词」的输入固定为同案同独立权利要求最新成功的模块三「总结核心发明点」输出（发明画像 + 技术特征），不再重读专利摘要/说明书/权利要求原文、不再重新调用 GLM 生成画像，改为免模型确定性重编译（输出携带 `source_plan_run_id` 溯源）；无对应成功模块三运行时模块四明确失败（`LAB_I2_PROFILE_REQUIRED`），禁止静默回退；2026-08-02 禁复用守门收窄为只拦 `plan_queries` 引擎内部缓存复用，不拦该显式重编译路径；正式 I0 流水线的 I2 行为不变。 |
| 2.7 | 2026-08-04 | 按用户决定：律师模块实验室原业务模块三「检索方案」拆分为两个相互独立的业务模块——新模块三「总结核心发明点」（新底层模块 `I2_INVENTIVE_PROFILE`，输出 AI 核心发明点总结一段话、发明点区别特征及其对应技术特征，不生成检索式）和新模块四「生成检索关键词」（沿用 `I2_QUERY_PLAN` 原行为，内部独立重新生成画像并编译查询，不读取模块三结果）；业务模块总数由六个变为七个，原四至六模块顺延为五至七；I0–I5 底层模块边界不变。 |
| 2.6 | 2026-08-03 | 按用户决定：可执行检索关键词原子粒度冻结——单个中文关键词不超过 6 个字，超限必须提炼；中文客体使用核心类别名词（剥离修饰定语）配同义 OR 组，修饰语只能拆成独立 AND 关键词；权利要求整句禁止作为检索词，必须提炼为原子技术名词 AND 组合；英文仍保留完整短语规则。 |
| 2.5 | 2026-08-02 | 按用户决定：首轮检索词硬上限为 AI 判断最有希望的 5 条，按确定性优先级保留、被裁线入 `portfolio_warnings` 可审计；纯动作族辅助效果线降级、抵触申请通道在上限内折叠、单发明点画像不生成重复双发明点线；确认并核查模块三禁止案件特定锁定，泛化历史单案件效果正则。 |
| 2.4 | 2026-08-02 | 按用户决定：测试阶段禁止 I2 同源缓存画像复用，模块实验室每次点击必须重新生成检索方案；测试服务对 `same_source_cached_profile` fail closed，复用路径仅保留给正式 release 与离线脚本。 |
| 2.3 | 2026-07-31 | 细化 I4-S 整体机构对应与必然隐含披露：不要求名称、零件划分或外形逐字同一；按结构角色、路径、拓扑、运动/状态和控制关系判断，允许集成构件承接多个目标特征，并要求用可核查的必要性链排除合理替代路径。 |
| 2.2 | 2026-07-30 | I2 可执行 feature anchor 改为原子技术关键词：禁止把说明句或顿号/逗号并列的多个概念作为一个关键词；并列概念拆为不同组，位置限定只在需要时独立 AND，完整单一技术术语仍可整体保留。 |
| 2.1 | 2026-07-30 | 收紧 I2 机构等价词：可执行主锚点使用目标原词/可回溯发明点短语；等价词仅作同一动作机理内的受控 OR 扩展；禁止由选择性联接自行推导锁定/解锁，并要求历史缓存画像重编译前清理。 |
| 2.0 | 2026-07-30 | 在搜索命中与全文取文之间增加 I3-F 候选清理：供应商原始前 10 全部留痕；同申请/同公开号版本/可靠同族确定性合并；仅排除有充分题录证据的明显跨领域候选，不确定项失败开放保留；清理后的唯一代表全部取文并逐篇分析。 |
| 1.9 | 2026-07-29 | I2 增加可审计的机构作用/动作等价词组；未知分类角色按目标专利已有分类号有界分线；P002 禁止把完整客体缩成宽词及用自动结构碎片稀释作用词；冻结 D1 前十回归通过。 |
| 1.8 | 2026-07-29 | 模块三改为“最大相似度长精确线 + 多条短召回线”的多分辨率组合；长线允许四个以上高区分度概念且零命中不算失败；冻结决定书 D1 的首轮前十盲测门槛。 |
| 1.7 | 2026-07-29 | 冻结通用机构语义检索与决定书盲测隔离；增加可读全文/异步 OCR 的 `analysis_ready` 守门；I4-S 改为整体结构角色、拓扑和工作过程映射标准。 |
| 1.6 | 2026-07-27 | 冻结模块四/五完整处理口径：每条专利检索前 10 篇全部取文、取得全文后逐篇 I4-S；页面不得以部分成功冒充全部完成；明确未披露与证据不足状态分离并展示权利要求特征原文。 |
| 1.5 | 2026-07-27 | 专利取文增加受控 fallback：EPO OPS 对同一候选明确无覆盖/无媒体时，以同一智慧芽 `patent_id + pn` 调用 P020；冻结触发条件、身份校验、证据升级和双 provider provenance。 |
| 1.4 | 2026-07-26 | 专利 provider 分工改为“智慧芽 P002 候选发现 + EPO OPS 按返回公开号/申请号核验并补取同一文献全文”；冻结身份映射、凭据隔离和证据升级边界。 |
| 1.3 | 2026-07-26 | 首轮可执行检索式改为“技术特征关键词、保护客体/类别、分类号三类任二”；分类号允许作为有标记的检索建议；I2 单次生成不合规时自动重生，连续失败时确定性组装。 |
| 1.2 | 2026-07-26 | 首轮检索从固定“主题+两个必要特征”改为保护客体/类别、类别共有语境、发明点区别特征三角色，并把标题摘要、权利要求、说明书全文及分类号范围纳入查询合同。 |
| 1.1 | 2026-07-25 | 当前自动无效检索范围收敛为独立权利要求；从属权利要求仅作为冻结事实保存，不创建调查或影响汇总/报告；律师模块实验室改为真实案件/内置示例两种输入。 |
| 1.0 | 2026-07-25 | 根据用户决定，关键日由逐权利要求人工核验改为专利级单一关键日（有优先权日取最早优先权日，否则申请日），自动适用于全部权利要求；仅专利级日期缺失或无效时转人工；多优先权逐主题规则延后。 |
| 0.9 | 2026-07-23 | 确认智慧芽为当前及后续专利候选发现默认通路；冻结 P002 双语分组查询、每组语言平衡、首次零命中后的单次新查询及 `lead` 证据边界。 |
| 0.8 | 2026-07-22 | 根据用户决定，将当前主专利 provider 候选切换为智慧芽（Patsnap），暂时停用 USPTO/EPO live；保留既有 adapter 作为后续可选路径，并要求智慧芽 API 文档、许可和真实样本验收。 |
| 0.6 | 2026-07-19 | 区分读取顺序与按作用域的规则权威；明确无效工作流规范、原侵权 AGENTS 和模块说明不会形成并列主文件；冻结 D1/D2/D3 的正式选择顺序。 |
| 0.5 | 2026-07-19 | 冻结唯一文档权威层级；补充双检索线、D1 版本化、四类 gap、抵触申请独立发现通道、轮次原子收口和人工继续规则。 |
| 0.4 | 2026-07-19 | 记录用户开始开发授权，并把指定实例专利目录的全量、逐件、失败修正后重跑设为完成门槛。 |
| 0.3 | 2026-07-19 | 明确宪章与现有 AGENTS 的适用范围/优先级；将无效检索改为逐权利要求、最接近现有技术和区别特征驱动的多轮工作流。 |
| 0.2 | 2026-07-19 | 取消对第三方来源、Google Patents 和浏览器自动化的一刀切禁令，改为真实文件与证据资格分层；明确开发前数据源验证门。 |
| 0.1 | 2026-07-19 | 建立项目目标、用户、MVP 架构、依赖限制、范围及功能修改触发标准。 |
